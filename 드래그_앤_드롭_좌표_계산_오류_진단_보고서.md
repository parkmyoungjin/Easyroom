# 드래그 앤 드롭 좌표 계산 오류 최종 진단을 위한 데이터 분석 보고서

**TO:** 대표님  
**FROM:** 개발팀  
**DATE:** 2025년 8월 6일  
**RE:** **요일 변경 드래그 시 발생하는 시간 계산 오류의 근본 원인 규명을 위한 최종 데이터 분석 보고서**

---

## 📋 **목표**

`handleDragEnd` 함수가 실행될 때, `dnd-kit` 라이브러리가 제공하는 이벤트 객체(`DragEndEvent`)의 실제 데이터를 확인함으로써, **"요일 변경 드래그 시 시간이 08:00로 계산되는"** 현상의 정확한 원인을 파악한다.

---

## 🔍 **1. '감시 카메라' 설치: `handleDragEnd` 데이터 로깅**

### **목표**
`handleDragEnd` 함수가 실행되는 순간의 모든 관련 데이터를 `console.log`로 출력하여, 우리가 어떤 값을 가지고 계산을 시작하는지 확인합니다.

### **1-1. `GoogleCalendarView.tsx` 코드 수정**

**요청:** `src/features/reservation/components/GoogleCalendarView.tsx` 파일의 `handleDragEnd` 함수를 찾아서, **함수의 가장 첫 부분**에 아래의 `console.log` 코드를 추가해 주십시오.

```tsx
// FILE: src/features/reservation/components/GoogleCalendarView.tsx

const handleDragEnd = (event: DragEndEvent) => {
  // ✅ [진단 코드] 이 로그를 함수 맨 위에 추가합니다.
  console.log("🕵️ [DND 진단] handleDragEnd Event Object:", event);
  console.log("🕵️ [DND 진단] event.active:", event.active);
  console.log("🕵️ [DND 진단] event.over:", event.over);
  console.log("🕵️ [DND 진단] event.delta:", event.delta);
  console.log("🕵️ [DND 진단] event.activatorEvent:", event.activatorEvent);
  
  const { active, over, delta } = event; // delta도 함께 확인
  setDraggedReservation(null);

  // ... (함수의 나머지 부분은 그대로 둡니다)
};
```

---

## 🧪 **2. '실행 기록' 확보: 두 가지 시나리오 테스트**

### **목표**
'성공하는 경우'와 '실패하는 경우'의 데이터를 비교하여, 어떤 값의 차이가 문제를 일으키는지 찾아냅니다.

### **2-1. 테스트 시나리오 수행**

**요청:** 브라우저에서 개발자 도구(F12)를 열고 **'Console' 탭**을 선택한 상태에서, 아래 두 가지 행동을 순서대로 수행해 주십시오.

#### **시나리오 A (성공 케이스)**
- **같은 날짜 안에서** 예약 블록 하나를 다른 시간으로 드래그 앤 드롭합니다.
- 예: 월요일 10:00 예약을 → 월요일 14:00으로 이동
- 이때는 정상적으로 14:00에 배치되어야 합니다.

#### **시나리오 B (실패 케이스)**
- **다른 날짜로** 예약 블록 하나를 드래그 앤 드롭합니다.
- 예: 월요일 10:00 예약을 → 화요일 14:00 위치로 이동
- 이때 08:00로 이동하는 현상이 재현되어야 합니다.

### **2-2. 로그 데이터 제출**

**요청:** 위 두 시나리오를 수행한 후, 브라우저의 콘솔 탭에 출력된 `🕵️ [DND 진단] ...` 로그 **두 개**를 모두 복사하여 제공해 주십시오.

**중요:** 각 로그 객체 옆에는 펼치기(▶) 버튼이 있습니다. 이 버튼을 눌러 **객체의 모든 하위 속성(`active`, `over`, `delta`, `activatorEvent` 등)이 보이도록 펼친 후** 복사해주시면, 가장 완벽한 정보를 얻을 수 있습니다.

---

## 📊 **3. 예상 분석 포인트**

이 두 가지 시나리오의 **`event` 객체 데이터**만 있으면, 저는 다음과 같은 질문에 100% 확신을 가지고 답변할 수 있습니다:

### **3-1. 좌표 계산 관련**
- `over.rect` (드롭 영역의 좌표) 값이 두 시나리오에서 모두 올바르게 들어오는가?
- `over.rect.top`, `over.rect.left`, `over.rect.width`, `over.rect.height` 값들이 예상 범위 내에 있는가?

### **3-2. 이동 거리 계산 관련**
- `event.delta` (이동 거리) 값이 두 시나리오에서 어떻게 다르게 계산되는가?
- `delta.x`, `delta.y` 값이 실제 마우스 이동 거리와 일치하는가?

### **3-3. 마우스 이벤트 관련**
- `event.activatorEvent` (마우스 이벤트)의 `clientY` 값이 두 시나리오에서 신뢰할 수 있는 값을 제공하는가?
- `activatorEvent.clientX`, `activatorEvent.clientY` 값이 실제 클릭 위치와 일치하는가?

### **3-4. 드롭 영역 식별 관련**
- `over.id` 값이 올바른 요일 컬럼을 가리키고 있는가? (예: `day-0`, `day-1`, `day-2` 등)
- `active.id` 값이 드래그된 예약의 올바른 ID를 가지고 있는가?

---

## 🎯 **4. 기대 결과**

이 데이터를 바탕으로, 우리는 더 이상 **'추측'**이 아닌 **'계산'**을 통해, 이 문제의 근본 원인을 제거하는 완벽한 코드 수정안을 도출할 수 있을 것입니다.

### **가능한 원인 가설들**
1. **좌표 계산 오류**: `over.rect` 값이 잘못 계산되어 항상 상단(08:00)을 가리킴
2. **이벤트 델타 오류**: `event.delta` 값이 다른 날짜로 이동할 때 부정확하게 계산됨
3. **드롭 영역 인식 오류**: 다른 날짜 컬럼의 `over.rect`가 실제 드롭 위치와 다름
4. **상대 좌표 계산 오류**: `relativeY` 계산 시 기준점이 잘못 설정됨

### **해결 방향**
- 정확한 원인 파악 후, 해당 부분의 좌표 계산 로직을 수정
- 필요시 `dnd-kit`의 다른 API 활용 (예: `collisionDetection` 커스터마이징)
- 브라우저별 호환성 문제 해결

---

## 📝 **제출 양식**

아래 형식으로 테스트 결과를 제출해 주십시오:

```
=== 시나리오 A (성공 케이스) ===
🕵️ [DND 진단] handleDragEnd Event Object: {
  // 여기에 전체 event 객체 내용 붙여넣기
}

🕵️ [DND 진단] event.active: {
  // 여기에 active 객체 내용 붙여넣기
}

🕵️ [DND 진단] event.over: {
  // 여기에 over 객체 내용 붙여넣기
}

🕵️ [DND 진단] event.delta: {
  // 여기에 delta 객체 내용 붙여넣기
}

=== 시나리오 B (실패 케이스) ===
🕵️ [DND 진단] handleDragEnd Event Object: {
  // 여기에 전체 event 객체 내용 붙여넣기
}

🕵️ [DND 진단] event.active: {
  // 여기에 active 객체 내용 붙여넣기
}

🕵️ [DND 진단] event.over: {
  // 여기에 over 객체 내용 붙여넣기
}

🕵️ [DND 진단] event.delta: {
  // 여기에 delta 객체 내용 붙여넣기
}
```

---

**이 정밀 진단을 통해 우리는 마침내 이 문제의 정확한 원인을 파악하고, 근본적인 해결책을 제시할 수 있을 것입니다.**