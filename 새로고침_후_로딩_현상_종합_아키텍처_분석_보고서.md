# 새로고침 후 로딩 현상 종합 아키텍처 분석 보고서

**TO:** 대표님  
**RE:** 새로고침 후 발생하는 페이지 로딩 정체 현상의 근본 원인 규명을 위한 최종 종합 분석 보고서  
**작성일:** 2025년 8월 6일 (수)

---

## 목표
애플리케이션을 새로고침하거나 처음 방문했을 때, 페이지가 로딩 화면에서 넘어가지 않는 현상의 근본 원인을 찾기 위해, **사용자 요청부터 최종 UI 렌더링까지의 전체 경로**를 추적하고 분석한다.

---

## 1. '교통 시스템'의 전체 설계도 (The Blueprints)

### 1-1. 최전선 교통 경찰 (`middleware.ts`)

**현재 상태:** ✅ 정상 동작
```typescript
// 핵심 로직: 모든 요청에 대해 세션 갱신만 수행
export async function middleware(request: NextRequest) {
  // Supabase 서버 클라이언트 생성
  const supabase = createServerClient(/* ... */);
  
  // 세션 갱신을 위해 호출 (결과는 사용하지 않음)
  await supabase.auth.getUser();
  
  return response;
}

// 적용 범위: API, 정적 파일 제외한 모든 경로
export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

**분석:**
- ✅ 리디렉션 로직 없음 (단순 세션 갱신만)
- ✅ 모든 경로에서 통과 허용
- ⚠️ **잠재적 이슈:** `createServerClient` deprecated 경고 존재

---

### 1-2. 중앙 관제탑 (`AuthContext.tsx`)

**현재 상태:** ⚠️ 복잡한 로직 + 타입 에러 존재

**핵심 상태 관리:**
```typescript
const [user, setUser] = useState<User | null>(initialSession?.user ?? null);
const [userProfile, setUserProfile] = useState<UserProfile | null>(null);
const [isLoading, setIsLoading] = useState(true); // ✅ 명시적 로딩 상태

// 파생 상태로 authStatus 계산
const authStatus: AuthStatus = isLoading
  ? 'loading'
  : user && userProfile ? 'authenticated' : 'unauthenticated';
```

**초기화 로직:**
```typescript
useEffect(() => {
  if (initialSession) {
    // 서버에서 세션 주입된 경우
    fetchProfile(initialSession.user);
    setIsLoading(false);
  } else {
    // PWA/CSR 환경: 현재 세션 확인
    supabase.auth.getSession().then(({ data: { session } }) => {
      if (session) {
        fetchProfile(session.user);
      }
      setIsLoading(false);
    });
  }
}, [supabase, initialSession]);
```

**분석:**
- ✅ 로딩 상태 명시적 관리
- ⚠️ **잠재적 병목:** `getOrCreateProfile` RPC 호출
- ❌ **타입 에러:** `supabase`가 null일 수 있음
- ❌ **논리적 모순:** `initialSession`이 있어도 `fetchProfile` 실패 시 `authenticated` 상태가 되지 않음

---

### 1-3. 교통 규칙 담당관 (`AuthGatekeeper.tsx`)

**현재 상태:** ✅ 명확한 라우팅 규칙

**핵심 라우팅 로직:**
```typescript
useEffect(() => {
  if (authStatus === 'loading') return; // 로딩 중에는 대기

  const publicRoutes = ['/', '/welcome', '/login', '/signup', '/reservations/status'];
  const authRoutes = ['/welcome', '/login', '/signup'];

  if (authStatus === 'unauthenticated') {
    // 비로그인: 공개 경로가 아니면 /welcome로
    const isPublicRoute = publicRoutes.includes(pathname) || pathname.startsWith('/kiosk');
    if (!isPublicRoute) {
      router.replace('/welcome');
    }
  } else if (authStatus === 'authenticated') {
    // 로그인: / → /dashboard, 인증 페이지 → /dashboard
    if (pathname === '/') {
      router.replace('/dashboard');
    }
    if (authRoutes.includes(pathname)) {
      router.replace('/dashboard');
    }
  }
}, [authStatus, pathname, router, userProfile]);

// 로딩 중 UI
if (authStatus === 'loading') {
  return <AuthGatekeeperLoader />;
}
```

**분석:**
- ✅ 로딩 상태에서 적절한 UI 표시
- ✅ 명확한 라우팅 규칙
- ⚠️ **잠재적 이슈:** `authStatus`가 'loading'에서 벗어나지 못하면 영구 로딩

---

### 1-4. 도시의 입구 (`layout.tsx`)

**현재 상태:** ✅ 서버 세션 주입 정상

**핵심 구조:**
```typescript
export default async function RootLayout({ children }) {
  // 서버에서 세션 가져오기
  const supabase = await createClient();
  const { data: { session } } = await supabase.auth.getSession();
  
  return (
    <SupabaseProvider>
      <AuthProvider initialSession={session}>
        <AuthGatekeeper>
          {children}
        </AuthGatekeeper>
      </AuthProvider>
    </SupabaseProvider>
  );
}
```

**분석:**
- ✅ 서버에서 세션을 가져와 `AuthProvider`에 주입
- ✅ 컴포넌트 계층 구조 명확

---

### 1-5. 목적지 페이지들

**루트 페이지 (`/`):**
```typescript
export default function HomePage() {
  return (
    <Center mih="100vh">
      <Loader size="lg" />
      <Text>페이지를 준비하고 있습니다...</Text>
    </Center>
  );
}
```

**웰컴 페이지 (`/welcome`):**
```typescript
export default function WelcomePage() {
  return (
    <Center mih="100vh">
      {/* 로그인/회원가입 버튼들 */}
    </Center>
  );
}
```

**분석:**
- ✅ 루트 페이지는 단순 로딩 UI
- ✅ 웰컴 페이지는 정적 콘텐츠

---

## 2. 현재 시스템 흐름 분석

### 정상적인 흐름 (예상)
1. **사용자 요청** → `/` 경로 접속
2. **Middleware** → 세션 갱신 후 통과
3. **Layout** → 서버에서 세션 가져와서 `AuthProvider`에 주입
4. **AuthProvider** → `initialSession` 존재 시 프로필 로드 후 `isLoading = false`
5. **AuthGatekeeper** → `authStatus`에 따라 라우팅 결정
6. **최종 렌더링** → 적절한 페이지 표시

### 🚨 잠재적 병목 지점들

#### A. AuthContext의 프로필 로딩 실패
```typescript
// 이 RPC 호출이 실패하거나 지연되면?
const profile = await getOrCreateProfile(supabase);
```
- **증상:** `userProfile`이 null로 남아있음
- **결과:** `user`는 있지만 `userProfile`이 없어서 `authStatus = 'unauthenticated'`
- **최종:** 무한 로딩 또는 잘못된 라우팅

#### B. Supabase 클라이언트 null 상태
```typescript
// 타입 에러가 실제 런타임 에러로 이어질 수 있음
supabase.auth.getSession() // supabase가 null이면?
```

#### C. 세션과 프로필의 비동기 불일치
- 서버에서 `session`을 가져왔지만
- 클라이언트에서 `getOrCreateProfile`이 실패하면
- `user`는 있지만 `userProfile`은 없는 상태

---

## 3. 진단을 위해 필요한 실행 기록

### 3-1. 서버 측 실행 기록 요청
**절차:**
1. 현재 개발 서버를 `Ctrl + C`로 완전 종료
2. `npm run dev` 실행하여 서버 재시작
3. 브라우저에서 캐시 비우고 강력 새로고침 (`Ctrl+Shift+R`)으로 루트 경로(`/`) 접속
4. 로딩 현상 발생 후, 터미널에 출력된 **모든 로그** 제공

### 3-2. 클라이언트 측 실행 기록 요청
**절차:**
1. 위와 동일한 절차를 브라우저 개발자 도구(F12) → Console 탭 열린 상태에서 진행
2. 로딩 현상 발생 후, 브라우저 콘솔에 출력된 **모든 로그** 제공

---

## 4. 예상 진단 시나리오

### 시나리오 A: RPC 함수 실패
**로그 예상:**
```
[AuthContext] Exception in getOrCreateProfile. Error: ...
```
**원인:** `get_or_create_user_profile` RPC 함수 문제
**해결:** RPC 함수 수정 또는 대체 로직 구현

### 시나리오 B: Supabase 연결 문제
**로그 예상:**
```
TypeError: Cannot read properties of null (reading 'auth')
```
**원인:** `SupabaseProvider`에서 클라이언트 생성 실패
**해결:** Supabase 설정 및 환경변수 확인

### 시나리오 C: 세션 불일치
**로그 예상:**
```
AuthProvider: initialSession exists but profile fetch failed
```
**원인:** 서버 세션은 유효하지만 클라이언트에서 프로필 로드 실패
**해결:** 에러 핸들링 개선 및 fallback 로직 추가

---

## 5. 결론

현재 시스템의 설계도를 분석한 결과, **AuthContext의 프로필 로딩 과정**이 가장 유력한 병목 지점으로 판단됩니다. 

**핵심 의심 지점:**
1. `getOrCreateProfile` RPC 호출 실패/지연
2. `supabase` 클라이언트 null 상태
3. 세션과 프로필 간의 비동기 불일치

위에서 요청한 **실행 기록 2개**를 확보하면, 정확히 어느 지점에서 흐름이 막히는지 밀리초 단위로 추적하여 근본 원인을 규명할 수 있습니다.

---

**다음 단계:** 실행 기록 제공 후 정밀 진단 및 해결책 제시