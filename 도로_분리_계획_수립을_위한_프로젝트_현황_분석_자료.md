# '도로 분리 계획' 수립을 위한 프로젝트 현황 분석 자료

## 1. 분석 목표

현재 애플리케이션의 **인증 상태에 따른 라우팅 및 UI 렌더링 방식**을 완벽히 파악하여, `/` 경로를 `/welcome`과 `/dashboard`로 분리하는 최적의 전략을 함께 설계하기 위함입니다.

## 2. 핵심 라우팅 및 UI 분기점

### 가. `src/app/page.tsx` - 현재 앱의 진입점

**현재 구조 분석:**

```typescript
export default function HomePage() {
  const { authStatus, userProfile } = useAuth();

  // 1. 로딩 상태 처리
  if (authStatus === 'loading') {
    return null;
  }

  // 2. 인증된 사용자: 개인화된 메인 허브 표시
  if (authStatus === 'authenticated' && userProfile) {
    return <AuthenticatedLanding userName={userProfile.name} userProfile={userProfile} />;
  }

  // 3. 비인증 사용자: 로그인/가입 선택 화면 표시
  if (authStatus === 'unauthenticated') {
    return <UnauthenticatedLanding />;
  }

  return null;
}
```

**핵심 발견사항:**
- 현재 `/` 경로는 **"상태 기반의 카멜레온"** 방식으로 동작
- `authStatus`에 따라 완전히 다른 두 개의 컴포넌트를 조건부 렌더링:
  - `AuthenticatedLanding`: 로그인한 사용자용 대시보드 (Bento Grid 스타일)
  - `UnauthenticatedLanding`: 비로그인 사용자용 환영 페이지
- 로그아웃 후 **페이지 리디렉션이 없음** - 상태 변경에만 의존

**AuthenticatedLanding 주요 기능:**
- 사용자 환영 메시지 및 프로필 정보 표시
- Bento Grid 스타일의 기능 카드들:
  - 새 예약하기 (`/reservations/new`)
  - 내 예약 보기 (`/reservations/my`)
  - 예약 현황 (`/reservations/status`)
  - 전체 대시보드 (`/dashboard`)
- 관리자 전용 메뉴 (조건부 표시)
- 로그아웃 버튼 (현재 리디렉션 없음)

**UnauthenticatedLanding 주요 기능:**
- EasyRoom 브랜드 소개
- 로그인/회원가입 버튼
- 비로그인 사용자도 접근 가능한 "전체 예약 현황 둘러보기" 링크

## 3. 서버-사이드 경로 통제 시스템

### 나. `middleware.ts` - 모든 요청의 최전선 문지기

**현재 보호 규칙:**

```typescript
// 라우팅 규칙 정의
const protectedRoutes = ['/reservations/my', '/reservations/new', '/reservations/edit', '/admin'];
const authRoutes = ['/login', '/signup'];

// 시나리오 1: 비로그인 사용자가 보호된 경로 접근 시도
if (!user && protectedRoutes.some(route => pathname.startsWith(route))) {
  return NextResponse.redirect(new URL('/login', request.url));
}

// 시나리오 2: 로그인한 사용자가 인증 전용 경로 접근 시도
if (user && authRoutes.some(route => pathname.startsWith(route))) {
  return NextResponse.redirect(new URL('/', request.url));
}
```

**핵심 발견사항:**
- **`/` 경로는 보호되지 않음** - 모든 사용자가 접근 가능
- 로그인한 사용자가 `/login`, `/signup` 접근 시 → `/`로 리디렉션
- 비로그인 사용자가 보호된 경로 접근 시 → `/login`으로 리디렉션
- 현재 `/dashboard`, `/welcome` 경로는 정의되지 않음

## 4. 인증 상태의 '심장부'

### 다. `src/contexts/AuthContext.tsx` - 전역 인증 상태 관리

**상태 관리 구조:**

```typescript
export type AuthStatus = 'loading' | 'authenticated' | 'unauthenticated';

interface AuthContextType {
  user: User | null;
  userProfile: UserProfile | null;
  authStatus: AuthStatus;
}
```

**onAuthStateChange 핸들러:**

```typescript
const { data: { subscription } } = supabase.auth.onAuthStateChange(
  async (event: AuthChangeEvent, session: SupabaseSession | null) => {
    console.log(`[AuthProvider] Event received: ${event}`);

    if (session) {
      // 세션 존재 시: 프로필 조회/생성 후 authenticated 상태로 전환
      const profile = await getOrCreateProfile(supabase);
      if (profile) {
        setUser(session.user);
        setUserProfile(profile);
        setAuthStatus('authenticated');
      } else {
        // 프로필 실패 시 강제 로그아웃
        await supabase.auth.signOut();
        setUser(null);
        setUserProfile(null);
        setAuthStatus('unauthenticated');
      }
    } else {
      // 세션 없음 시: unauthenticated 상태로 전환
      setUser(null);
      setUserProfile(null);
      setAuthStatus('unauthenticated');
    }
  }
);
```

**핵심 발견사항:**
- 모든 인증 상태 변경은 `onAuthStateChange` 이벤트를 통해 처리
- 상태 변경 시 **자동 페이지 리디렉션 없음** - 컴포넌트 리렌더링에만 의존
- 프로필 조회 실패 시 자동 로그아웃 처리

## 5. 상태 변경을 유발하는 주요 기능들

### 라. `src/hooks/useAuth.ts` - 인증 액션 함수들

**로그아웃 함수:**

```typescript
const signOut = useCallback(async () => {
  if (!supabase) return;
  
  console.log('[useAuth] Starting logout process');
  
  try {
    const { error } = await supabase.auth.signOut();
    
    if (error) {
      console.error('[useAuth] Logout error:', error);
      throw error;
    }

    console.log('[useAuth] Logout completed, onAuthStateChange will handle cleanup');
  } catch (error) {
    console.error('[useAuth] Logout failed:', error);
    throw error;
  }
}, [supabase]);
```

**핵심 발견사항:**
- 로그아웃 시 `supabase.auth.signOut()` 호출
- **명시적 페이지 리디렉션 없음** - `onAuthStateChange`에 의존
- 에러 발생 시 예외 던짐

### 마. 로그인 폼 컴포넌트 - `src/features/auth/components/LoginForm.tsx`

**로그인 성공 처리:**

```typescript
const onOTPSubmit = useCallback(async (data: OTPVerificationFormData) => {
  try {
    await verifyOTP(userEmail, data.otp);
    
    // 성공 시 상태만 변경, 리디렉션 없음
    setCurrentStep('success');
    toast.success('로그인 성공!', {
      description: '잠시 후 메인 페이지로 이동합니다.',
    });
  } catch (error) {
    setError('OTP 인증 중 오류가 발생했습니다.');
  }
});
```

**로그인 페이지의 리디렉션 처리:**

```typescript
// src/app/login/page.tsx
useEffect(() => {
  // 인증 완료시 즉시 리다이렉트
  if (authStatus === 'authenticated' && userProfile) {
    console.log('[LoginPage] Authentication detected, redirecting to main page');
    router.replace('/');
  }
}, [authStatus, userProfile, router]);
```

**핵심 발견사항:**
- 로그인 성공 시 `verifyOTP` 함수가 `onAuthStateChange` 이벤트 트리거
- 로그인 페이지에서만 `useEffect`를 통한 명시적 리디렉션 구현
- 메인 페이지(`/`)에서는 리디렉션 로직 없음

## 6. 전체 구조도

### 바. `src/app` 디렉토리 구조

```
src/app/
├── admin/
│   └── page.tsx
├── api/
│   ├── admin/
│   ├── auth/
│   ├── deployment-info/
│   ├── health/
│   ├── monitoring/
│   ├── reservations/
│   └── rooms/
├── auth/
│   ├── callback/
│   └── error/
├── dashboard/
│   └── page.tsx
├── login/
│   └── page.tsx
├── reservations/
│   ├── browse/
│   ├── edit/
│   ├── my/
│   ├── new/
│   └── status/
├── signup/
│   └── page.tsx
├── test-loading/
├── favicon.ico
├── globals.css
├── layout.tsx
├── page.tsx          # 현재 메인 진입점
├── providers.tsx
└── viewport.ts
```

**핵심 발견사항:**
- `/dashboard` 경로가 이미 존재하지만 별도 페이지로 구현됨
- `/welcome` 경로는 존재하지 않음
- 현재 `/` 경로가 두 가지 역할을 모두 수행 중

## 7. 🔍 현재 시스템의 문제점 및 개선 기회

### 문제점 1: 로그아웃 후 페이지 리디렉션 부재
- 로그아웃 시 같은 페이지에 머물러 있어 사용자 혼란 야기
- 상태 변경에만 의존하는 구조로 인한 UX 문제

### 문제점 2: 단일 경로의 이중 역할
- `/` 경로가 환영 페이지와 대시보드 역할을 동시에 수행
- SEO 및 북마크 관점에서 불리
- 사용자가 특정 상태의 페이지를 직접 접근하기 어려움

### 문제점 3: 라우팅 로직의 분산
- 메인 페이지: 조건부 렌더링
- 로그인 페이지: `useEffect` 리디렉션
- 미들웨어: 서버 사이드 보호
- 일관성 없는 라우팅 패턴

### 개선 기회 1: 명확한 경로 분리
- `/welcome`: 비로그인 사용자 전용
- `/dashboard`: 로그인 사용자 전용
- `/`: 적절한 경로로 리디렉션하는 라우터 역할

### 개선 기회 2: 일관된 리디렉션 패턴
- 모든 인증 상태 변경 시 명시적 페이지 이동
- 미들웨어와 클라이언트 사이드 라우팅의 일관성 확보

### 개선 기회 3: 향상된 사용자 경험
- 북마크 가능한 명확한 URL
- 브라우저 뒤로가기 버튼 지원
- SEO 최적화

## 8. 📋 다음 단계를 위한 설계 고려사항

### 고려사항 1: 기존 코드 호환성
- 현재 `AuthenticatedLanding`과 `UnauthenticatedLanding` 컴포넌트 재사용
- 기존 라우팅 로직 최소 변경
- 점진적 마이그레이션 가능성

### 고려사항 2: 미들웨어 규칙 업데이트
- `/welcome` 경로 보호 규칙 추가
- `/dashboard` 경로 보호 규칙 추가
- `/` 경로의 새로운 역할 정의

### 고려사항 3: 상태 관리 개선
- `AuthContext`에서 리디렉션 로직 추가 여부
- `useAuth` 훅에서 라우팅 헬퍼 함수 제공 여부
- 전역 라우팅 상태 관리 필요성

이제 이 분석 자료를 바탕으로 함께 최적의 '도로 분리 계획'을 수립할 준비가 완료되었습니다.