# 🔍 "뒤로 가기 버튼" 표시 문제 분석 보고서

**작성일:** 2025-08-06  
**분석 대상:** AppHeader 컴포넌트의 뒤로 가기 버튼 표시 로직  
**목표:** 특정 페이지에서 뒤로 가기 버튼이 보이지 않는 근본 원인 파악

---

## 📋 1단계: 헤더 설계도 분석 (The Blueprint)

### AppHeader 컴포넌트 구조 분석

#### Props 인터페이스
```typescript
interface AppHeaderProps {
  title?: string;
  subtitle?: string;
  showBackButton?: boolean;  // ✅ 명시적 제어 가능
  showHomeButton?: boolean;
  onBack?: () => void;
  rightContent?: React.ReactNode;
  variant?: 'default' | 'minimal' | 'kiosk' | 'admin';
  showLogo?: boolean;
}
```

#### 뒤로 가기 버튼 표시 로직
```typescript
const shouldShowBackButton = () => {
  // 1. 명시적 설정이 있으면 우선 사용
  if (showBackButton !== undefined) {
    return showBackButton;
  }

  // 2. minimal 변형에서는 표시하지 않음
  if (variant === 'minimal') {
    return false;
  }

  // 3. 메인 페이지들 (뒤로 가기 불필요)
  const mainPages = ['/', '/dashboard', '/welcome'];
  if (mainPages.includes(pathname)) {
    return false;
  }

  // 4. 서브 페이지들 (뒤로 가기 필요)
  const subPagePatterns = [
    '/reservations/new',
    '/reservations/edit/',
    '/reservations/my',
    '/reservations/status',
    '/reservations/browse',
    '/kiosk/',
    '/admin'
  ];

  return subPagePatterns.some(pattern => pathname.startsWith(pattern));
};
```

#### 렌더링 구조
```typescript
{shouldShowBackButton() && (
  <Button
    variant="subtle"
    size="compact-sm"
    onClick={handleBack}
    leftSection={<ArrowLeft size={16} />}
  >
    뒤로
  </Button>
)}
```

---

## 📋 2단계: 실제 사용 현황 분석 (The Implementation)

### 각 페이지별 AppHeader 사용 방식

#### 1. `/reservations/new` - 새 예약 페이지
```typescript
// ❌ 문제 발견: showBackButton prop을 전달하지 않음
<AppLayout headerTitle="새 예약">
```
- **현재 상태:** `showBackButton` prop 누락
- **자동 판단 결과:** ✅ `/reservations/new`가 `subPagePatterns`에 포함되어 있어 자동으로 `true` 반환
- **예상 동작:** 뒤로 가기 버튼이 표시되어야 함

#### 2. `/reservations/edit/[id]` - 예약 수정 페이지
```typescript
// ❌ 문제 발견: showBackButton prop을 전달하지 않음
<AppLayout headerTitle="예약 수정">
```
- **현재 상태:** `showBackButton` prop 누락
- **자동 판단 결과:** ✅ `/reservations/edit/`가 `subPagePatterns`에 포함되어 있어 자동으로 `true` 반환
- **예상 동작:** 뒤로 가기 버튼이 표시되어야 함

#### 3. `/reservations/my` - 내 예약 관리 페이지
```typescript
// ❌ 문제 발견: showBackButton prop을 전달하지 않음
<AppLayout headerTitle="내 예약 관리">
```
- **현재 상태:** `showBackButton` prop 누락
- **자동 판단 결과:** ✅ `/reservations/my`가 `subPagePatterns`에 포함되어 있어 자동으로 `true` 반환
- **예상 동작:** 뒤로 가기 버튼이 표시되어야 함

#### 4. `/reservations/status` - 전체 예약 현황 페이지
```typescript
// ❌ 문제 발견: showBackButton prop을 전달하지 않음
<AppLayout headerTitle="전체 예약 현황">
```
- **현재 상태:** `showBackButton` prop 누락
- **자동 판단 결과:** ✅ `/reservations/status`가 `subPagePatterns`에 포함되어 있어 자동으로 `true` 반환
- **예상 동작:** 뒤로 가기 버튼이 표시되어야 함

### AppLayout 중간 계층 분석
```typescript
// AppLayout.tsx에서 기본값 설정
showBackButton = false,  // ⚠️ 잠재적 문제점 발견!
```

---

## 🚨 근본 원인 발견!

### 핵심 문제점
**AppLayout 컴포넌트에서 `showBackButton`의 기본값을 `false`로 설정**하고 있어, 페이지에서 명시적으로 `showBackButton={true}`를 전달하지 않으면 AppHeader의 자동 판단 로직이 무시됩니다.

### 문제 발생 흐름
1. 페이지에서 `<AppLayout headerTitle="새 예약">` 호출 (showBackButton 누락)
2. AppLayout에서 `showBackButton = false` (기본값) 적용
3. AppHeader에서 `showBackButton !== undefined` 조건에 의해 `false` 반환
4. **자동 경로 판단 로직(`shouldShowBackButton`)이 실행되지 않음**
5. 결과: 뒤로 가기 버튼이 표시되지 않음

---

## 💡 해결 방안

### 방안 1: AppLayout 기본값 수정 (권장)
```typescript
// AppLayout.tsx 수정
showBackButton,  // undefined로 변경 (기본값 제거)
```

### 방안 2: 각 페이지에서 명시적 설정
```typescript
// 각 페이지에서
<AppLayout headerTitle="새 예약" showBackButton={true}>
```

### 방안 3: AppHeader 로직 개선
```typescript
// showBackButton이 false일 때도 자동 판단 로직 실행
const shouldShowBackButton = () => {
  // showBackButton이 명시적으로 true인 경우
  if (showBackButton === true) {
    return true;
  }
  
  // showBackButton이 명시적으로 false인 경우
  if (showBackButton === false) {
    return false;
  }
  
  // undefined인 경우에만 자동 판단 실행
  // ... 기존 자동 판단 로직
};
```

---

## 🎯 권장 해결책

**방안 1 (AppLayout 기본값 수정)**을 권장합니다. 이는:
- 기존 자동 판단 로직을 최대한 활용
- 각 페이지 코드 수정 최소화
- 향후 새로운 페이지에서도 자동으로 올바른 동작 보장

### 구체적 수정 사항
```typescript
// src/components/layout/AppLayout.tsx
export default function AppLayout({
  children,
  headerTitle,
  headerSubtitle,
  showBackButton, // = false 제거
  // ... 나머지 props
}: AppLayoutProps) {
```

이 수정만으로 모든 예약 관련 페이지에서 뒤로 가기 버튼이 자동으로 표시될 것입니다.