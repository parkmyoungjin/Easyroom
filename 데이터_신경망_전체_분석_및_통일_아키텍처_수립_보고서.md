# 데이터 신경망 전체 분석 및 통일 아키텍처 수립 보고서

**TO:** 대표님  
**RE:** 애플리케이션 전체의 데이터 조회 경로('통신 회선')를 시각화하고, 이를 단일하고 예측 가능한 아키텍처로 통일하기 위한 최종 종합 분석 보고서  
**작성일:** 2025년 8월 7일

---

## 📋 목표

현재 애플리케이션 내 모든 페이지와 컴포넌트가 **어떤 경로와 방식**으로 Supabase로부터 데이터를 가져오는지 **'전체 데이터 흐름도'**를 작성한다. 이 흐름도를 바탕으로, 현재 발생하는 '실시간 동기화 실패'와 같은 문제의 근본 원인(예: 통신 방식의 불일치)을 정확히 진단하고, 모든 데이터 조회가 **하나의 일관된 '통신 표준'**을 따르도록 시스템 전체를 재설계한다.

---

## 🔍 1단계: 모든 '데이터 소비자'와 그들의 '공급망' 식별

### 1.1 새 예약 폼 (`ReservationForm.tsx`)

**파일 위치:** `src/components/reservations/ReservationForm.tsx`

**사용하는 데이터 훅:**
- `useRooms()` - 회의실 목록 조회
- `useBookedSlots(roomId, date)` - 특정 회의실의 예약된 시간 슬롯 조회
- `useMyReservations(userProfile?.dbId)` - 내 예약 목록 (수정 모드용)

**통신 회선 분석:**
```typescript
// useBookedSlots 내부 구조
export function useBookedSlots(roomId: string | null, date: Date | null) {
  return useQuery({
    queryKey: roomKeys.bookedSlots(roomId || '', dateKey),
    queryFn: () => {
      // ✅ 서비스 계층 완전 위임 - RoomService.getInstance().getBookedSlots()
      return RoomService.getInstance().getBookedSlots(supabase, roomId, date);
    },
    enabled: !!roomId && !!date && !!supabase,
  });
}
```

**통신 방식:** **RPC 직접 호출** (서비스 계층을 통해)  
**실시간 구독:** ❌ (필요 없음 - 폼 입력 중 실시간 업데이트 불필요)

---

### 1.2 내 예약 페이지 (`/reservations/my`)

**파일 위치:** `src/app/reservations/my/page.tsx`

**사용하는 데이터 훅:**
- `useMyReservations(userProfile?.dbId)` - 내 예약 목록 조회

**통신 회선 분석:**
```typescript
// useMyReservations 내부 구조
export function useMyReservations(userId: string | undefined) {
  return useQuery({
    queryKey: reservationKeys.my(userId),
    queryFn: () => {
      // ✅ 서비스 계층 위임 - reservationService.getMyReservations()
      return reservationService.getMyReservations(supabase, userId);
    },
    enabled: authStatus === 'authenticated' && !!userId && !!supabase,
  });
}
```

**통신 방식:** **RPC 직접 호출** (서비스 계층을 통해)  
**실시간 구독:** ❌ (개인 데이터이므로 실시간 필요성 낮음)  
**문제점:** 데이터가 0개로 나오는 문제 발생 (로그에서 확인됨)

---

### 1.3 전체 예약 현황 페이지 (`/reservations/status`)

**파일 위치:** `src/app/reservations/status/page.tsx`

**사용하는 데이터 훅:**
- `usePublicReservations(startDate, endDate, isAuthenticated, currentUserId)` - 공개 예약 목록 조회

**통신 회선 분석:**
```typescript
// usePublicReservations 내부 구조
export function usePublicReservations(startDate: string, endDate: string, isAuthenticated?: boolean, currentUserId?: string) {
  const queryResult = useQuery({
    queryKey: reservationKeys.public(startDate, endDate, isAuthenticated),
    queryFn: () => {
      // ✅ 서비스 계층 위임 - reservationService.getPublicReservations()
      return reservationService.getPublicReservations(startDate, endDate, isAuthenticated);
    },
  });

  // ✅ 실시간 구독 설정
  useEffect(() => {
    const unsubscribe = realtimeManager.subscribeToPublicReservations(
      supabase, queryClient, queryKey, startDate, endDate, currentUserId
    );
    return unsubscribe;
  }, [supabase, queryClient, queryKey, startDate, endDate, currentUserId]);
}
```

**통신 방식:** **API 라우트 경유** + **실시간 구독**  
**실시간 구독:** ✅ (realtimeManager를 통한 Postgres Changes 구독)  
**문제점:** **실시간 업데이트 실패의 직접적 원인** - API 라우트와 실시간 구독 간 데이터 불일치

---

### 1.4 키오스크 페이지 (`/kiosk/room-display`)

**파일 위치:** `src/app/kiosk/room-display/page.tsx`

**사용하는 데이터 훅:**
- `usePublicReservations(startDate, endDate, !!userProfile, userProfile?.dbId)` - 오늘의 예약 데이터

**통신 회선 분석:**
```typescript
// 키오스크에서도 동일한 usePublicReservations 사용
const { data: todayReservations, isError } = usePublicReservations(
  startDateStr, 
  endDateStr, 
  !!userProfile,
  userProfile?.dbId
);
```

**통신 방식:** **API 라우트 경유** + **실시간 구독**  
**실시간 구독:** ✅ (전체 예약 현황과 동일한 구독 방식)  
**문제점:** 전체 예약 현황과 동일한 실시간 동기화 문제 공유

---

### 1.5 대시보드 페이지 (`/dashboard`)

**파일 위치:** `src/app/dashboard/page.tsx`

**사용하는 데이터 훅:**
- 직접적인 데이터 조회 없음 (단순 네비게이션 허브)

**ReservationDashboard 컴포넌트:**
```typescript
// src/features/reservation/components/ReservationDashboard.tsx
const { data: reservations, isLoading, isError } = usePublicReservations(
  startDateStr,
  endDateStr,
  true // 항상 public 데이터 사용
);
```

**통신 방식:** **API 라우트 경유** + **실시간 구독**  
**실시간 구독:** ✅ (전체 예약 현황과 동일)

---

### 1.6 API 라우트 핸들러

**파일 위치:** `src/app/api/reservations/public-authenticated/route.ts`

**통신 회선 분석:**
```typescript
// API 라우트 내부 - RPC 함수 직접 호출
const { data, error } = await supabase.rpc('get_public_reservations_with_room', {
  start_date: normalizedStartDate,
  end_date: normalizedEndDate,
  page_limit: limit,
  page_offset: offset
});
```

**통신 방식:** **RPC 직접 호출** (서버 사이드에서)  
**역할:** 클라이언트와 Supabase 간 중간 계층 역할

---

## 🗺️ 2단계: '데이터 신경망 지도' 작성

| 데이터 소비자 (페이지) | 사용하는 훅 | 통신 회선 방식 | 실시간 구독 | 문제점/특이사항 |
|:---|:---|:---|:---|:---|
| **새 예약 폼** | `useBookedSlots` | **RPC 직접 호출** | ❌ | 정상 작동 |
| **내 예약** | `useMyReservations` | **RPC 직접 호출** | ❌ | **데이터 0개 문제 발생** |
| **전체 예약 현황** | `usePublicReservations` | **API 라우트 경유** | ✅ | **실시간 업데이트 실패** |
| **키오스크** | `usePublicReservations` | **API 라우트 경유** | ✅ | 전체 예약 현황과 동일 문제 |
| **대시보드** | `usePublicReservations` | **API 라우트 경유** | ✅ | 전체 예약 현황과 동일 문제 |

### 🚨 핵심 문제점 진단

#### 문제 1: **통신 방식의 불일치**
- **개인 데이터 조회**: RPC 직접 호출 방식 사용
- **공개 데이터 조회**: API 라우트 경유 방식 사용
- **결과**: 데이터 조회 방식이 혼재되어 일관성 부족

#### 문제 2: **실시간 구독과 초기 데이터 조회 간 불일치**
- **초기 데이터**: API 라우트 → RPC 함수 호출
- **실시간 업데이트**: Postgres Changes 직접 구독
- **결과**: 두 데이터 소스 간 변환 로직 차이로 인한 동기화 실패

#### 문제 3: **데이터 변환 로직의 분산**
- API 라우트에서 한 번 변환
- realtimeManager에서 또 다른 변환
- 두 변환 결과가 일치하지 않음

---

## 🔧 3단계: '통신 표준' 수립 및 해결책 제시

### 3.1 통일된 통신 표준 수립

#### **표준 1: 실시간 필요 데이터 (공개 예약)**
- **대상**: 전체 예약 현황, 키오스크, 대시보드
- **통신 방식**: **RPC 직접 호출** (API 라우트 제거)
- **실시간 구독**: Postgres Changes 구독 유지
- **이유**: 초기 데이터와 실시간 업데이트의 데이터 소스 통일

#### **표준 2: 실시간 불필요 데이터 (개인 예약)**
- **대상**: 내 예약, 새 예약 폼
- **통신 방식**: **RPC 직접 호출** 유지
- **실시간 구독**: 불필요
- **이유**: 개인 데이터는 실시간 동기화 필요성 낮음

### 3.2 구체적 해결 방안

#### 방안 1: API 라우트 제거 및 RPC 직접 호출 통일
```typescript
// 수정 전 (문제 있는 방식)
export function usePublicReservations(startDate: string, endDate: string) {
  return useQuery({
    queryFn: () => reservationService.getPublicReservations(startDate, endDate), // API 라우트 경유
  });
}

// 수정 후 (통일된 방식)
export function usePublicReservations(startDate: string, endDate: string) {
  return useQuery({
    queryFn: () => {
      // RPC 직접 호출로 통일
      return supabase.rpc('get_public_reservations_with_room', {
        start_date: startDate,
        end_date: endDate
      });
    },
  });
}
```

#### 방안 2: 실시간 구독 데이터 변환 로직 통일
```typescript
// realtimeManager 내부 수정
private async transformToPublicReservation(dbRow: any, currentUserId?: string) {
  // ✅ RPC 함수와 동일한 변환 로직 적용
  // API 라우트의 변환 로직을 제거하고 여기서 통일
}
```

#### 방안 3: 서비스 계층에서 통신 방식 통일
```typescript
// reservationService 수정
class ReservationService {
  async getPublicReservations(startDate: string, endDate: string) {
    // API 라우트 호출 제거
    // RPC 직접 호출로 변경
    const { data, error } = await this.supabase.rpc('get_public_reservations_with_room', {
      start_date: startDate,
      end_date: endDate
    });
    
    if (error) throw error;
    return data;
  }
}
```

### 3.3 마이그레이션 계획

#### Phase 1: API 라우트 제거 (우선순위: 높음)
1. `usePublicReservations` 훅에서 API 라우트 호출 제거
2. RPC 직접 호출로 변경
3. 기존 API 라우트 파일 삭제

#### Phase 2: 데이터 변환 로직 통일 (우선순위: 높음)
1. realtimeManager의 변환 로직을 RPC 함수 결과와 일치시킴
2. 중복된 변환 로직 제거

#### Phase 3: 테스트 및 검증 (우선순위: 중간)
1. 실시간 동기화 정상 작동 확인
2. 모든 페이지에서 데이터 일관성 검증

---

## 📊 4단계: 예상 효과

### 4.1 문제 해결 효과
- **실시간 동기화 실패 해결**: 초기 데이터와 실시간 업데이트 데이터 소스 통일
- **데이터 일관성 확보**: 모든 페이지에서 동일한 데이터 조회 방식 사용
- **성능 향상**: API 라우트 중간 계층 제거로 응답 속도 개선

### 4.2 아키텍처 개선 효과
- **단순화**: 복잡한 API 라우트 계층 제거
- **예측 가능성**: 모든 데이터 조회가 동일한 패턴 따름
- **유지보수성**: 데이터 변환 로직 중앙화

---

## 🎯 결론

현재 애플리케이션의 **가장 큰 문제는 '통신 회선의 불일치'**입니다. 

- **개인 데이터**는 RPC 직접 호출
- **공개 데이터**는 API 라우트 경유
- **실시간 업데이트**는 Postgres Changes 직접 구독

이 세 가지 서로 다른 통신 방식이 혼재하면서, 특히 **공개 데이터의 실시간 동기화에서 치명적인 불일치**가 발생하고 있습니다.

**해결책은 명확합니다**: 모든 데이터 조회를 **RPC 직접 호출**로 통일하고, 실시간 구독의 데이터 변환 로직을 RPC 함수 결과와 일치시키는 것입니다.

이를 통해 우리는 마침내 **"어떤 페이지는 실시간이 되고, 어떤 페이지는 안 되는"** 혼란을 끝내고, 모든 데이터 조회가 **명확한 의도와 규칙**에 따라 움직이는 완벽한 시스템을 구축할 수 있습니다.

---

**다음 단계**: 대표님의 승인 후 즉시 Phase 1 마이그레이션 작업을 시작하겠습니다.