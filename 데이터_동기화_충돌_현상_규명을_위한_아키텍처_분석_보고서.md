# 데이터 동기화 충돌 현상 규명을 위한 아키텍처 분석 보고서

**TO:** 대표님  
**RE:** 모바일 환경에서 발생하는 'UI 업데이트 지연' 현상의 근본 원인 규명을 위한, '실시간 구독'과 '캐시 무효화' 신호 체계 종합 분석 보고서

---

## 목표

'예약 수정' 성공 후, **`usePublicReservations`** (실시간 구독)의 '수동 캐시 업데이트'와 **`useUpdateReservation`** (뮤테이션)의 '캐시 무효화 및 자동 Refetch'라는 **두 개의 다른 데이터 동기화 신호**가 어떻게 상호작용(또는 충돌)하는지 명확하게 파악한다. 이를 통해, 모바일 환경에서 UI 업데이트가 지연되는 현상의 근본 원인을 찾아내고, 두 신호 체계를 조화롭게 만드는 가장 안전한 해결책을 설계한다.

---

## 1. 신호 A (실시간 구독)의 설계도

### 1-1. `usePublicReservations` 훅 (실시간 구독 호출부)

**파일:** `src/hooks/useReservations.ts`

```typescript
// 공개 예약을 가져오는 훅 (실시간 동기화 포함) - Phase 2: DI 패턴 적용
export function usePublicReservations(startDate: string, endDate: string, isAuthenticated?: boolean, currentUserId?: string) {
  const supabase = useSupabaseClient();
  const queryClient = useQueryClient();
  const { authStatus } = useAuthContext();
  
  const queryKey = reservationKeys.public(startDate, endDate, isAuthenticated);
  const dateOptimization = optimizeForDateRange(startDate, endDate);
  
  const queryResult = useQuery({
    queryKey,
    queryFn: () => {
      return reservationService.getPublicReservations(startDate, endDate, isAuthenticated);
    },
    enabled: !!startDate && !!endDate,
    staleTime: 0,  // ⚠️ 중요: 항상 stale 상태로 유지
    gcTime: dateOptimization.gcTime,
    retry: 2,
    retryDelay: 1000
  });

  // ✅ Phase 2: 실시간 구독 설정 - 별도 매니저로 단순화
  useEffect(() => {
    if (!supabase || !startDate || !endDate) return;

    // ✅ 복잡한 실시간 구독 로직을 별도 매니저에 위임
    const unsubscribe = realtimeManager.subscribeToPublicReservations(
      supabase,
      queryClient,
      queryKey,
      startDate,
      endDate,
      currentUserId
    );

    // ✅ 클린업 함수 반환
    return unsubscribe;
  }, [supabase, queryClient, queryKey, startDate, endDate, currentUserId]);

  return queryResult;
}
```

**핵심 분석 포인트:**
- **staleTime: 0** → 캐시가 항상 stale 상태로 유지됨
- **실시간 구독**이 `realtimeManager.subscribeToPublicReservations`를 통해 설정됨
- **queryKey**: `reservationKeys.public(startDate, endDate, isAuthenticated)`

### 1-2. `realtimeManager.subscribeToPublicReservations` (실시간 구독 핵심 로직)

**파일:** `src/lib/subscriptions/realtimeManager.ts`

```typescript
subscribeToPublicReservations(
  supabase: SupabaseClient,
  queryClient: QueryClient,
  queryKey: readonly unknown[],
  startDate: string,
  endDate: string,
  currentUserId?: string
): () => void {
  const channelName = `public-reservations-${startDate}-${endDate}`;
  
  // 이미 구독 중인 채널이 있다면 제거
  if (this.activeChannels.has(channelName)) {
    this.unsubscribe(channelName);
  }

  const channel = supabase
    .channel(channelName)
    .on(
      'postgres_changes',
      {
        event: '*',
        schema: 'public',
        table: 'reservations',
        // 서버사이드 필터: 현재 조회 중인 날짜 범위만 수신
        filter: `start_time.gte.${startDate}T00:00:00Z,start_time.lt.${endDate}T23:59:59Z`
      },
      async (payload) => {
        // 현재 캐시 데이터 가져오기
        const currentData = queryClient.getQueryData<PublicReservation[]>(queryKey);
        if (!currentData) return;

        // 이벤트 타입별 캐시 업데이트
        let updatedData: PublicReservation[] = [...currentData];

        switch (payload.eventType) {
          case 'INSERT': {
            // 새 예약 추가 로직
            break;
          }
          case 'UPDATE': {
            // 예약 수정 로직 - ⚠️ 여기서 수동 캐시 업데이트 발생
            if (payload.new) {
              const updatedReservation = await this.transformToPublicReservation(
                payload.new, 
                currentUserId, 
                supabase
              );
              if (updatedReservation && this.isWithinDateRange(updatedReservation, startDate, endDate)) {
                const index = updatedData.findIndex(item => item.id === updatedReservation.id);
                if (index !== -1) {
                  updatedData[index] = updatedReservation;  // ⚠️ 수동 업데이트
                }
              }
            }
            break;
          }
          case 'DELETE': {
            // 예약 삭제 로직
            break;
          }
        }

        // ⚠️ 핵심: 캐시 수동 업데이트
        queryClient.setQueryData(queryKey, updatedData);
      }
    )
    .subscribe();

  return () => {
    this.unsubscribe(channelName);
  };
}
```

**핵심 분석 포인트:**
- **수동 캐시 업데이트**: `queryClient.setQueryData(queryKey, updatedData)`
- **UPDATE 이벤트**에서 예약 수정 시 즉시 캐시를 수동으로 업데이트
- **동일한 queryKey** 사용: `reservationKeys.public(startDate, endDate, isAuthenticated)`

---

## 2. 신호 B (캐시 무효화)의 설계도

### 2-1. `useUpdateReservation` 훅 (뮤테이션)

**파일:** `src/hooks/useReservations.ts`

```typescript
// ✅ Phase 2: 예약을 수정하는 뮤테이션 훅 - 데이터 변환 로직 서비스 계층 이전 (침범도: 20% → 0%)
export function useUpdateReservation() {
  const queryClient = useQueryClient();
  const supabase = useSupabaseClient();

  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: Partial<ReservationFormData> }) => {
      if (!supabase) {
        throw new Error('인증 컨텍스트를 사용할 수 없어 예약을 수정할 수 없습니다.');
      }
      
      // ✅ 데이터 변환 로직을 서비스 계층으로 완전 위임
      return reservationService.updateReservation(supabase, id, data);
    },
    onSuccess: (updatedReservation) => {
      // ⚠️ 핵심: 캐시 무효화만 유지 (상태 관리 책임)
      queryClient.invalidateQueries({ queryKey: reservationKeys.all });
      // ✅ UI 피드백은 컴포넌트 레벨에서 처리하도록 제거 예정
    },
    onError: (error) => {
      // ✅ 에러 로깅은 서비스 계층에서 이미 처리됨, 여기서는 에러만 전파
      throw error;
    },
  });
}
```

**핵심 분석 포인트:**
- **캐시 무효화**: `queryClient.invalidateQueries({ queryKey: reservationKeys.all })`
- **무효화 범위**: `reservationKeys.all` → **모든 예약 관련 쿼리**를 무효화
- **자동 Refetch**: `staleTime: 0`이므로 무효화 후 즉시 refetch 시작

### 2-2. 쿼리 키(Query Key) 팩토리

**파일:** `src/lib/queryKeys.ts`

```typescript
/**
 * 예약(Reservation) 관련 쿼리 키 팩토리
 */
export const reservationKeys = {
  all: ['reservations'] as const,  // ⚠️ 최상위 키
  lists: () => [...reservationKeys.all, 'list'] as const,
  list: (filters: string) => [...reservationKeys.lists(), filters] as const,
  my: (userId: string | undefined) => [...reservationKeys.all, 'my', userId] as const,
  public: (startDate: string, endDate: string, isAuthenticated?: boolean) =>
    [...reservationKeys.all, 'public', startDate, endDate, 'auth', isAuthenticated] as const,  // ⚠️ 실시간 구독이 사용하는 키
  withDetails: (startDate: string, endDate: string) =>
    [...reservationKeys.all, 'withDetails', startDate, endDate] as const,
  statistics: (startDate: string, endDate: string) =>
    [...reservationKeys.all, 'statistics', startDate, endDate] as const,
  detail: (id: string) => [...reservationKeys.all, 'detail', id] as const,
};
```

**핵심 분석 포인트:**
- **계층 구조**: `reservationKeys.public`은 `reservationKeys.all`을 포함
- **무효화 범위**: `invalidateQueries({ queryKey: reservationKeys.all })`는 **모든 하위 키**를 무효화
- **타겟 쿼리**: `reservationKeys.public(startDate, endDate, isAuthenticated)`도 무효화 대상에 포함

---

## 3. 신호 충돌 분석 결과

### 3-1. 두 신호 체계의 상호작용 패턴

1. **사용자가 예약 수정 실행**
2. **`useUpdateReservation` 뮤테이션 성공**
3. **신호 B 발동**: `queryClient.invalidateQueries({ queryKey: reservationKeys.all })`
4. **자동 Refetch 시작**: `staleTime: 0`이므로 즉시 서버에서 최신 데이터 요청
5. **동시에 신호 A 발동**: Supabase 실시간 구독이 UPDATE 이벤트 수신
6. **수동 캐시 업데이트**: `queryClient.setQueryData(queryKey, updatedData)`

### 3-2. 경쟁 상태(Race Condition) 발생 지점

⚠️ **핵심 문제**: 두 신호가 **동일한 queryKey**를 타겟으로 하여 **동시에** 캐시를 업데이트하려고 시도

```
Timeline:
T0: 예약 수정 요청
T1: 뮤테이션 성공 → invalidateQueries 실행 → Refetch 시작
T2: 실시간 구독 UPDATE 이벤트 수신 → setQueryData 실행
T3: Refetch 완료 → 서버 데이터로 캐시 덮어쓰기

문제: T2와 T3의 순서가 불확실함
- 모바일: 네트워크 지연으로 T3가 늦어져서 T2 결과가 T3에 의해 덮어써질 가능성
- 데스크탑: 빠른 네트워크로 T3가 먼저 완료되어 정상 동작
```

### 3-3. 모바일 환경에서 지연 발생 원인

1. **네트워크 지연**: 모바일 환경의 느린 네트워크로 인해 Refetch가 지연됨
2. **타이밍 불일치**: 실시간 구독의 수동 업데이트가 Refetch 결과보다 먼저 적용되지만, 이후 Refetch 결과가 이를 덮어씀
3. **캐시 경합**: 두 신호가 동일한 캐시 슬롯을 동시에 업데이트하려고 시도

---

## 4. 해결책 설계 방향

### 4-1. 안전한 해결책 옵션

**옵션 1: 실시간 구독 우선 전략**
- `useUpdateReservation`에서 `invalidateQueries` 제거
- 실시간 구독의 수동 캐시 업데이트만 사용
- 장점: 경쟁 상태 완전 제거
- 단점: 실시간 구독 실패 시 UI 업데이트 안됨

**옵션 2: 뮤테이션 우선 전략**
- 실시간 구독에서 자신이 수정한 예약은 업데이트 스킵
- `currentUserId`와 `payload.new.user_id` 비교하여 필터링
- 장점: 뮤테이션 결과 우선 보장
- 단점: 실시간 구독 로직 복잡화

**옵션 3: 지연 실행 전략**
- 실시간 구독 업데이트를 일정 시간(예: 1초) 지연
- Refetch가 먼저 완료되도록 보장
- 장점: 기존 로직 최소 변경
- 단점: 실시간성 저하

### 4-2. 권장 해결책

**"뮤테이션 우선 + 스마트 필터링" 전략**을 권장합니다:

1. **실시간 구독에서 자신의 수정 이벤트 필터링**
2. **뮤테이션의 invalidateQueries는 유지** (네트워크 실패 대비)
3. **실시간 구독은 다른 사용자의 변경사항만 처리**

이를 통해 두 신호 체계가 서로 다른 역할을 담당하게 하여 충돌을 원천 차단할 수 있습니다.

---

## 결론

현재 시스템의 **두 신호 체계가 동일한 캐시를 동시에 업데이트하려는 경쟁 상태**가 모바일 환경에서 UI 업데이트 지연의 근본 원인입니다. 네트워크 지연으로 인해 타이밍이 불일치하면서 실시간 구독의 수동 업데이트가 뮤테이션의 Refetch 결과에 의해 덮어써지는 현상이 발생하고 있습니다.

가장 안전한 해결책은 **역할 분리**를 통해 뮤테이션은 자신의 변경사항을, 실시간 구독은 다른 사용자의 변경사항만 처리하도록 하는 것입니다.