# 로그아웃 기능 이상 현상에 대한 긴급 진단 보고서

## 1. 현상 개요

사용자가 로그아웃 버튼을 클릭하면, 개발자 콘솔에 `[useAuth] Starting logout process` 메시지는 출력되지만 실제 로그아웃(세션 무효화, 페이지 리디렉션 등)은 완료되지 않는 문제가 발생하고 있습니다.

## 2. 진단 목적

문제의 원인이 **UI Layer**, **State Layer (Hook)**, **Service Layer** 중 어느 단계에 있는지, 혹은 각 단계 간의 상호작용에 있는지 명확히 규명하기 위함입니다. 우리의 '개발 헌법' 제1조(책임 분리)에 입각하여 각 계층의 동작을 검증합니다.

## 3. 코드 분석 결과

### 가. UI Layer: 액션의 시작점

**로그아웃 버튼 컴포넌트 (src/app/page.tsx)**

```typescript
// 로그아웃 핸들러 함수
const handleLogout = async () => {
  try {
    await signOut();
    toast.success('로그아웃 완료', {
      description: '안전하게 로그아웃되었습니다.',
    });
  } catch (error) {
    toast.error('로그아웃 오류', {
      description: '로그아웃 중 오류가 발생했습니다.',
    });
  }
};

// 로그아웃 버튼 JSX
<Button variant="outline" onClick={handleLogout} leftSection={<LogOut size={16} />}>
  <Text hiddenFrom="sm" visibleFrom="xs">로그아웃</Text>
  <Text visibleFrom="sm">로그아웃</Text>
</Button>
```

**✅ UI Layer 진단 결과:**
- `onClick` 이벤트 핸들러가 올바르게 `handleLogout` 함수를 호출
- `handleLogout` 함수가 `useAuth` 훅의 `signOut` 함수를 정상적으로 호출
- 에러 핸들링과 성공 토스트 메시지 로직이 구현되어 있음

### 나. State Layer: 상태 관리의 핵심

**useAuth 훅의 signOut 함수 (src/hooks/useAuth.ts)**

```typescript
const signOut = useCallback(async () => {
  if (!supabase) return;
  
  console.log('[useAuth] Starting logout process');
  
  try {
    // Simplified: Use standard signOut - auth-helpers handles all cookie management
    const { error } = await supabase.auth.signOut();
    
    if (error) {
      console.error('[useAuth] Logout error:', error);
      throw error;
    }

    // Simplified: Let onAuthStateChange handle all session cleanup
    // No manual cookie or storage clearing needed - auth-helpers handles this
    console.log('[useAuth] Logout completed, onAuthStateChange will handle cleanup');
  } catch (error) {
    console.error('[useAuth] Logout failed:', error);
    throw error;
  }
}, [supabase]);
```

**✅ State Layer 진단 결과:**
- `supabase.auth.signOut()` 호출이 정상적으로 구현됨
- 에러 핸들링이 적절히 구현됨
- `onAuthStateChange` 리스너에 세션 정리를 위임하는 구조

### 다. Service Layer: AuthContext의 onAuthStateChange 핸들러

**AuthContext의 상태 변경 리스너 (src/contexts/AuthContext.tsx)**

```typescript
const { data: { subscription } } = supabase.auth.onAuthStateChange(
  async (event: AuthChangeEvent, session: SupabaseSession | null) => {
    console.log(`[AuthProvider] Event received: ${event}`);

    if (session) {
      // 세션이 존재하면 (SIGNED_IN, TOKEN_REFRESHED, INITIAL_SESSION 등)
      const profile = await getOrCreateProfile(supabase);

      if (profile) {
        setUser(session.user);
        setUserProfile(profile);
        setAuthStatus('authenticated');
      } else {
        // 프로필 조회/생성 실패는 심각한 문제이므로 로그아웃 처리합니다.
        console.error("[AuthProvider] Profile fetch failed. Transitioning to unauthenticated.");
        await supabase.auth.signOut(); // 세션을 명확하게 종료
        setUser(null);
        setUserProfile(null);
        setAuthStatus('unauthenticated');
      }
    } else {
      // 세션이 없으면 (SIGNED_OUT)
      setUser(null);
      setUserProfile(null);
      setAuthStatus('unauthenticated');
    }
  }
);
```

**✅ Service Layer 진단 결과:**
- `SIGNED_OUT` 이벤트 발생 시 상태를 올바르게 정리하는 로직이 구현됨
- 사용자 상태(`user`), 프로필(`userProfile`), 인증 상태(`authStatus`)를 모두 초기화

## 4. 🔍 잠재적 문제점 분석

### 문제점 1: 페이지 리디렉션 부재
현재 로그아웃 로직에서는 **페이지 리디렉션이 구현되어 있지 않습니다.**

- `signOut` 함수는 세션 무효화만 수행
- `handleLogout` 함수에서도 성공 토스트만 표시하고 페이지 이동 없음
- 사용자가 로그아웃 후에도 같은 페이지에 머물러 있게 됨

### 문제점 2: 비동기 상태 업데이트 타이밍
`onAuthStateChange` 이벤트는 비동기적으로 발생하므로, 토스트 메시지가 상태 업데이트보다 먼저 표시될 수 있습니다.

### 문제점 3: 에러 처리 불완전
`supabase.auth.signOut()`에서 에러가 발생하지 않더라도, `onAuthStateChange` 이벤트가 정상적으로 발생하지 않을 가능성이 있습니다.

## 5. 🚨 긴급 수정 권장사항

### 권장사항 1: 명시적 페이지 리디렉션 추가

```typescript
const handleLogout = async () => {
  try {
    await signOut();
    toast.success('로그아웃 완료', {
      description: '안전하게 로그아웃되었습니다.',
    });
    // 🔧 추가: 로그아웃 후 홈페이지로 리디렉션
    router.push('/');
  } catch (error) {
    toast.error('로그아웃 오류', {
      description: '로그아웃 중 오류가 발생했습니다.',
    });
  }
};
```

### 권장사항 2: 상태 기반 조건부 렌더링 확인
현재 `HomePage` 컴포넌트에서 `authStatus`에 따른 조건부 렌더링이 구현되어 있으므로, 상태가 `unauthenticated`로 변경되면 자동으로 `UnauthenticatedLanding` 컴포넌트가 렌더링되어야 합니다.

### 권장사항 3: 디버깅을 위한 추가 로그
상태 변경 과정을 더 자세히 추적하기 위해 추가 로그를 권장합니다:

```typescript
// AuthContext.tsx에서
console.log(`[AuthProvider] State transition: ${authStatus} -> ${session ? 'authenticated' : 'unauthenticated'}`);
```

## 6. 🔬 추가 확인 필요 사항

### 브라우저 개발자 도구 확인 요청

**Console 탭에서 확인해야 할 로그:**
1. `[useAuth] Starting logout process`
2. `[useAuth] Logout completed, onAuthStateChange will handle cleanup`
3. `[AuthProvider] Event received: SIGNED_OUT`
4. `[AuthProvider] State transition: authenticated -> unauthenticated`

**Network 탭에서 확인해야 할 요청:**
1. Supabase auth 관련 요청 (`/auth/v1/logout` 또는 유사한 엔드포인트)
2. 요청의 상태 코드 (200, 204 등이 정상)

**Application 탭에서 확인해야 할 사항:**
1. Cookies: Supabase 관련 쿠키가 삭제되는지 확인
2. Local Storage: 인증 관련 데이터가 정리되는지 확인

## 7. 📋 결론 및 다음 단계

현재 코드 분석 결과, 로그아웃 로직 자체는 올바르게 구현되어 있습니다. 문제는 다음 중 하나일 가능성이 높습니다:

1. **페이지 리디렉션 부재**: 사용자가 로그아웃 후에도 같은 페이지에 머물러 있음
2. **비동기 상태 업데이트 지연**: `onAuthStateChange` 이벤트 처리가 지연되거나 실패
3. **브라우저 캐시 또는 쿠키 문제**: 클라이언트 측 저장소 정리 실패

위의 디버깅 정보를 수집한 후, 구체적인 해결책을 제시하겠습니다.