# [작전명: 아키텍처 현황 파악 (Operation: Architectural Assessment)]

**TO:** 프로젝트 책임자  
**FROM:** 기술 분석팀 (Kiro AI)  
**DATE:** 2025년 8월 5일  
**SUBJECT:** 4계층 아키텍처 동기화 상태 최종 진단을 위한 정보 분석

---

## 🎯 분석 목표

현재 애플리케이션의 데이터 관련 훅(use* hooks)들이 '서비스 계층'의 책임을 얼마나 포함하고 있는지 정량적으로 파악하여, 이상적인 4계층 아키텍처와의 차이(gap)를 명확히 정의한다.

---

## 📊 Part 1: 예약 관련 훅 분석 (`src/hooks/useReservations.ts`)

### 1.1 useMyReservations

```typescript
export function useMyReservations(userId: string | undefined): { data: ReservationWithDetails[] | undefined; isLoading: boolean; isError: boolean; error: any } {
  const { authStatus } = useAuthContext();
  const supabase = useSupabaseClient();

  const queryOptions = buildQueryOptions({
    queryKey: reservationKeys.my(userId),
    queryFn: createStandardFetch(
      async () => {
        if (authStatus !== 'authenticated' || !userId || !supabase) {
          return Promise.resolve([]);
        }
        
        // ✅ [레거시 RPC 함수 대체] get_reservations_for_period 통합 함수 사용
        const { data, error } = await supabase.rpc('get_reservations_for_period', {
          start_date: null, // 전체 기간 조회
          end_date: null
        });

        if (error) {
          logger.error('get_reservations_for_period RPC failed', error);
          throw new Error(`내 예약 조회 실패: ${error.message}`);
        }

        // ✅ [클라이언트 사이드 필터링] is_mine === true인 예약들만 필터링
        const myReservations = (data || []).filter((reservation: any) => reservation.is_mine === true);
        
        logger.debug('내 예약 조회 완료', { 
          totalReservations: data?.length || 0,
          myReservations: myReservations.length 
        });

        return myReservations as ReservationWithDetails[];
      },
      { operation: 'fetch my reservations (unified RPC)', params: { userId } }
    ),
    enabled: authStatus === 'authenticated' && !!userId && !!supabase,
    dataType: 'semi-static',
    cacheConfig: {
      customStaleTime: 0,
      customGcTime: 5 * 60 * 1000,
    }
  });

  return useQuery({
    ...queryOptions,
    refetchOnMount: 'always',
    refetchOnWindowFocus: true,
    refetchOnReconnect: true,
  });
}
```

**🔍 서비스 계층 책임 분석:**
- ❌ **직접 RPC 호출**: `supabase.rpc()` 직접 사용
- ❌ **비즈니스 로직 포함**: 클라이언트 사이드 필터링 (`is_mine === true`)
- ❌ **데이터 변환**: 타입 캐스팅 (`as ReservationWithDetails[]`)
- ❌ **에러 처리**: 도메인별 에러 메시지 생성
- ✅ **캐시 전략**: React Query 최적화 적용

**서비스 계층 침범도: 80%** (5개 중 4개 책임 포함)

### 1.2 usePublicReservations

```typescript
export function usePublicReservations(startDate: string, endDate: string, isAuthenticated?: boolean, currentUserId?: string) {
  const supabase = useSupabaseClient();
  const queryClient = useQueryClient();
  const { authStatus } = useAuthContext();
  
  const queryKey = reservationKeys.public(startDate, endDate, isAuthenticated);
  const dateOptimization = optimizeForDateRange(startDate, endDate);
  
  const queryResult = useQuery(buildQueryOptions({
    queryKey,
    queryFn: createStandardFetch(
      () => {
        if (authStatus !== 'authenticated') {
          return Promise.resolve([]);
        }
        return reservationService.getPublicReservations(startDate, endDate, isAuthenticated);
      },
      { operation: 'fetch public reservations', params: { startDate, endDate, isAuthenticated } }
    ),
    enabled: !!startDate && !!endDate && authStatus === 'authenticated',
    dataType: 'dynamic',
    cacheConfig: {
      customStaleTime: 0,
      customGcTime: dateOptimization.gcTime
    },
    retryConfig: {
      maxRetries: 2,
      baseDelay: 1000
    }
  }));

  // 실시간 구독 설정
  useEffect(() => {
    if (!supabase || !startDate || !endDate) return;

    const channelName = `public-reservations-${startDate}-${endDate}`;
    const channel = supabase
      .channel(channelName)
      .on(
        'postgres_changes',
        {
          event: '*',
          schema: 'public',
          table: 'reservations',
          filter: `start_time.gte.${startDate}T00:00:00Z,start_time.lt.${endDate}T23:59:59Z`
        },
        async (payload) => {
          logger.info('Realtime event received', { event: payload.eventType, table: payload.table });
          
          const currentData = queryClient.getQueryData<PublicReservation[]>(queryKey);
          if (!currentData) return;

          let updatedData: PublicReservation[] = [...currentData];

          switch (payload.eventType) {
            case 'INSERT': {
              if (payload.new) {
                const newReservation = await transformToPublicReservation(
                  payload.new, 
                  currentUserId, 
                  supabase
                );
                if (newReservation && isWithinDateRange(newReservation, startDate, endDate)) {
                  updatedData.push(newReservation);
                }
              }
              break;
            }
            case 'UPDATE': {
              if (payload.new) {
                const updatedReservation = await transformToPublicReservation(
                  payload.new, 
                  currentUserId, 
                  supabase
                );
                if (updatedReservation && isWithinDateRange(updatedReservation, startDate, endDate)) {
                  const index = updatedData.findIndex(item => item.id === updatedReservation.id);
                  if (index !== -1) {
                    updatedData[index] = updatedReservation;
                  } else {
                    updatedData.push(updatedReservation);
                  }
                } else {
                  updatedData = updatedData.filter(item => item.id !== payload.new.id);
                }
              }
              break;
            }
            case 'DELETE': {
              if (payload.old) {
                updatedData = updatedData.filter(item => item.id !== payload.old.id);
              }
              break;
            }
          }

          queryClient.setQueryData(queryKey, updatedData);
        }
      )
      .subscribe();

    return () => {
      supabase.removeChannel(channel);
    };
  }, [supabase, queryClient, queryKey, startDate, endDate, currentUserId]);

  return queryResult;
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `reservationService.getPublicReservations()` 사용
- ❌ **실시간 구독 로직**: 복잡한 Supabase 실시간 처리
- ❌ **데이터 변환**: `transformToPublicReservation()` 함수 호출
- ❌ **캐시 수동 조작**: `queryClient.setQueryData()` 직접 사용
- ❌ **비즈니스 로직**: 날짜 범위 검증 (`isWithinDateRange`)

**서비스 계층 침범도: 60%** (5개 중 3개 책임 포함)

### 1.3 useReservation (단일 예약 조회)

```typescript
export function useReservation(id: string) {
  const supabase = useSupabaseClient();
  const { authStatus } = useAuthContext();
  
  return useQuery(buildQueryOptions({
    queryKey: reservationKeys.detail(id),
    queryFn: createStandardFetch(
      () => {
        if (authStatus !== 'authenticated' || !supabase) {
          return Promise.resolve(null);
        }
        return reservationService.getReservationById(supabase, id);
      },
      { operation: 'fetch reservation by ID', params: { id } }
    ),
    enabled: !!id && authStatus === 'authenticated' && !!supabase,
    dataType: 'semi-static'
  }));
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `reservationService.getReservationById()` 사용
- ✅ **인증 상태 검증**: 적절한 가드 조건
- ✅ **캐시 전략**: React Query 최적화 적용

**서비스 계층 침범도: 0%** (완전한 계층 분리)

### 1.4 useAllReservations (관리자용)

```typescript
export function useAllReservations() {
  const supabase = useSupabaseClient();
  const { authStatus } = useAuthContext();
  
  return useQuery(buildQueryOptions({
    queryKey: reservationKeys.all,
    queryFn: createStandardFetch(
      () => {
        if (authStatus !== 'authenticated' || !supabase) {
          return Promise.resolve([]);
        }
        return reservationService.getAllReservations(supabase);
      },
      { operation: 'fetch all reservations (admin)', params: {} }
    ),
    enabled: authStatus === 'authenticated' && !!supabase,
    dataType: 'dynamic',
  }));
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `reservationService.getAllReservations()` 사용
- ✅ **인증 상태 검증**: 적절한 가드 조건
- ✅ **캐시 전략**: React Query 최적화 적용

**서비스 계층 침범도: 0%** (완전한 계층 분리)

---

## 🏢 Part 2: 회의실 관련 훅 분석 (`src/hooks/useRooms.ts`)

### 2.1 useRooms (활성 회의실 조회)

```typescript
export function useRooms() {
  const supabase = useSupabaseClient();
  const { authStatus } = useAuthContext();

  return useQuery(buildQueryOptions({
    queryKey: roomKeys.active(),
    queryFn: createStandardFetch(
      () => {
        if (authStatus !== 'authenticated' || !supabase) {
          return Promise.resolve([]);
        }
        return roomService.getActiveRooms(supabase);
      },
      {
        operation: 'fetch active rooms',
        params: {}
      }
    ),
    enabled: authStatus === 'authenticated' && !!supabase,
    dataType: 'static'
  }));
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `roomService.getActiveRooms()` 사용
- ✅ **인증 상태 검증**: 적절한 가드 조건
- ✅ **캐시 전략**: React Query 최적화 적용

**서비스 계층 침범도: 0%** (완전한 계층 분리)

### 2.2 useRoom (단일 회의실 조회)

```typescript
export function useRoom(id: string) {
  const supabase = useSupabaseClient();
  const { authStatus } = useAuthContext();

  return useQuery(buildQueryOptions({
    queryKey: roomKeys.detail(id),
    queryFn: createStandardFetch(
      () => {
        if (authStatus !== 'authenticated' || !supabase) {
          return Promise.resolve(null);
        }
        return roomService.getRoomById(supabase, id);
      },
      {
        operation: 'fetch room by ID',
        params: { id }
      }
    ),
    enabled: !!id && authStatus === 'authenticated' && !!supabase,
    dataType: 'static'
  }));
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `roomService.getRoomById()` 사용
- ✅ **인증 상태 검증**: 적절한 가드 조건
- ✅ **캐시 전략**: React Query 최적화 적용

**서비스 계층 침범도: 0%** (완전한 계층 분리)

### 2.3 useBookedSlots (특정 회의실의 예약된 시간 조회)

```typescript
export function useBookedSlots(roomId: string | null, date: Date | null) {
  const supabase = useSupabaseClient();
  const { authStatus } = useAuthContext();
  const dateKey = date ? format(date, 'yyyy-MM-dd') : '';

  return useQuery({
    queryKey: roomKeys.bookedSlots(roomId || '', dateKey),
    queryFn: async () => {
      if (authStatus !== 'authenticated' || !roomId || !date || !supabase) {
        return Promise.resolve([]);
      }

      // ✅ [레거시 RPC 함수 대체] get_reservations_for_period 통합 함수 사용
      const startDate = format(date, 'yyyy-MM-dd') + 'T00:00:00Z';
      const endDate = format(date, 'yyyy-MM-dd') + 'T23:59:59Z';

      const { data, error } = await supabase.rpc('get_reservations_for_period', {
        start_date: startDate,
        end_date: endDate
      });

      if (error) {
        logger.error('get_reservations_for_period RPC failed for booked slots', error);
        return [];
      }

      // ✅ [클라이언트 사이드 필터링] 특정 회의실의 예약들만 필터링
      const roomReservations = (data || []).filter((reservation: any) =>
        reservation.room_id === roomId && reservation.status !== 'cancelled'
      );

      // BookedSlot 형식으로 변환
      const bookedSlots = roomReservations.map((reservation: any) => ({
        id: reservation.id,
        start_time: reservation.start_time,
        end_time: reservation.end_time,
        title: reservation.title,
        user_name: reservation.user_name || '알 수 없음',
        is_mine: reservation.is_mine || false
      }));

      logger.debug('예약된 시간 슬롯 조회 완료', {
        roomId,
        dateKey,
        totalReservations: data?.length || 0,
        roomReservations: bookedSlots.length
      });

      return bookedSlots;
    },
    enabled: !!roomId && !!date && !!supabase && authStatus === 'authenticated',
    staleTime: 1 * 60 * 1000,
    gcTime: 5 * 60 * 1000,
  });
}
```

**🔍 서비스 계층 책임 분석:**
- ❌ **직접 RPC 호출**: `supabase.rpc()` 직접 사용
- ❌ **비즈니스 로직 포함**: 클라이언트 사이드 필터링 (회의실별, 상태별)
- ❌ **데이터 변환**: BookedSlot 형식으로 매핑
- ❌ **날짜 처리**: 날짜 포맷팅 로직
- ❌ **에러 처리**: 도메인별 에러 처리

**서비스 계층 침범도: 100%** (5개 중 5개 책임 포함)

### 2.4 useRoomAvailability (회의실 가용성 조회)

```typescript
export function useRoomAvailability(roomId: string, startDate: string, endDate: string) {
  const supabase = useSupabaseClient();
  const { authStatus } = useAuthContext();

  return useQuery(buildQueryOptions({
    queryKey: roomKeys.availability(roomId, startDate, endDate),
    queryFn: createStandardFetch(
      async () => {
        if (authStatus !== 'authenticated' || !supabase) {
          return Promise.resolve([]);
        }

        // Use optimized RPC function for detailed availability check
        const { data, error } = await supabase
          .rpc('get_room_availability_detailed', {
            room_id: roomId,
            start_time: new Date(startDate).toISOString(),
            end_time: new Date(endDate).toISOString()
          });

        if (error) {
          // Fallback to original service method
          return await roomService.getRoomAvailability(supabase, roomId, startDate, endDate);
        }

        return data;
      },
      {
        operation: 'check room availability',
        params: { roomId, startDate, endDate }
      }
    ),
    enabled: !!roomId && !!startDate && !!endDate && authStatus === 'authenticated' && !!supabase,
    dataType: 'real-time',
    cacheConfig: {
      customStaleTime: 1 * 60 * 1000,
      customGcTime: 5 * 60 * 1000
    }
  }));
}
```

**🔍 서비스 계층 책임 분석:**
- ❌ **직접 RPC 호출**: `supabase.rpc()` 직접 사용
- ❌ **폴백 로직**: 에러 시 서비스 계층으로 폴백
- ❌ **날짜 변환**: `new Date().toISOString()` 처리
- ✅ **캐시 전략**: React Query 최적화 적용

**서비스 계층 침범도: 60%** (5개 중 3개 책임 포함)

---

## 🔄 Part 3: 데이터 변경 훅 (Mutation Hooks) 분석

### 3.1 useCreateReservation

```typescript
export function useCreateReservation() {
  const queryClient = useQueryClient();
  const supabase = useSupabaseClient();

  return useMutation({
    mutationFn: async (data: ReservationInsert) => {
      if (!supabase) {
        logger.error('Supabase 클라이언트를 사용할 수 없습니다');
        throw new Error('Supabase 클라이언트를 사용할 수 없습니다. 페이지를 새로고침해주세요.');
      }
      
      logger.debug('Creating reservation with Supabase client');

      const result = await reservationService.createReservation(supabase, data); 
      
      logger.info('Reservation created successfully');
      return result;
    },
    onSuccess: () => {
      toast.success('예약 완료', {
        description: '회의실 예약이 성공적으로 완료되었습니다.',
      });
      queryClient.invalidateQueries({ queryKey: reservationKeys.all });
    },
    onError: (error: Error) => {
      logger.error('예약 생성 실패', error);
      toast.error('예약 실패', {
        description: error.message,
      });
    },
  });
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `reservationService.createReservation()` 사용
- ❌ **UI 피드백**: 토스트 메시지 처리
- ❌ **캐시 무효화**: 수동 캐시 관리
- ❌ **로깅**: 도메인별 로깅 처리

**서비스 계층 침범도: 40%** (5개 중 2개 책임 포함)

### 3.2 useUpdateReservation

```typescript
export function useUpdateReservation() {
  const queryClient = useQueryClient();
  const supabase = useSupabaseClient();

  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: Partial<ReservationFormData> }) => {
      if (!supabase) {
        throw new Error('인증 컨텍스트를 사용할 수 없어 예약을 수정할 수 없습니다.');
      }
      
      // Transform Date objects to ISO strings for database
      const updateData: Partial<ReservationUpdate> = {};
      if (data.title) {
        updateData.title = data.title;
      }
      if (data.purpose) {
        updateData.purpose = data.purpose;
      }
      if (data.start_time) {
        updateData.start_time = data.start_time.toISOString();
      }
      if (data.end_time) {
        updateData.end_time = data.end_time.toISOString();
      }
      
      return reservationService.updateReservation(supabase, id, updateData);
    },
    onSuccess: (updatedReservation) => {
      queryClient.invalidateQueries({ queryKey: reservationKeys.all });
      toast.success('예약이 수정되었습니다.');
    },
    onError: (error) => {
      const errorMessage = error instanceof Error ? error.message : '예약 수정 중 오류가 발생했습니다.';
      logger.error('예약 수정 실패', { error: errorMessage });
      toast.error('예약 수정 실패', {
        description: errorMessage,
      });
    },
  });
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `reservationService.updateReservation()` 사용
- ❌ **데이터 변환**: Date 객체를 ISO 문자열로 변환
- ❌ **UI 피드백**: 토스트 메시지 처리
- ❌ **캐시 무효화**: 수동 캐시 관리
- ❌ **에러 처리**: 도메인별 에러 메시지 생성

**서비스 계층 침범도: 60%** (5개 중 3개 책임 포함)

### 3.3 useCancelReservation

```typescript
export function useCancelReservation() {
  const queryClient = useQueryClient();
  const supabase = useSupabaseClient();

  return useMutation({
    mutationFn: ({ id, reason }: { id: string; reason?: string }) => {
      if (!supabase) throw new Error("인증 컨텍스트를 사용할 수 없습니다.");
      return reservationService.cancelReservation(supabase, id, reason);
    },
    onSuccess: () => {
      toast.success('예약이 취소되었습니다.');
      queryClient.invalidateQueries({ queryKey: reservationKeys.all });
    },
    onError: (error: Error) => {
      const errorMessage = error.message || '예약 취소 중 오류가 발생했습니다.';
      logger.error('예약 취소 실패', { error: errorMessage });
      toast.error('예약 취소 실패', { description: errorMessage });
    },
  });
}
```

**🔍 서비스 계층 책임 분석:**
- ✅ **서비스 계층 위임**: `reservationService.cancelReservation()` 사용
- ❌ **UI 피드백**: 토스트 메시지 처리
- ❌ **캐시 무효화**: 수동 캐시 관리
- ❌ **에러 처리**: 도메인별 에러 메시지 생성

**서비스 계층 침범도: 50%** (4개 중 2개 책임 포함)

---

## 📈 Part 4: 정량적 분석 결과

### 4.1 서비스 계층 침범도 통계

| 훅 분류 | 훅 이름 | 침범도 | 주요 침범 영역 |
|---------|---------|--------|----------------|
| **예약 조회** | useMyReservations | 80% | 직접 RPC 호출, 비즈니스 로직, 데이터 변환 |
| | usePublicReservations | 60% | 실시간 구독, 데이터 변환, 캐시 조작 |
| | useReservation | 0% | 완전한 계층 분리 |
| | useAllReservations | 0% | 완전한 계층 분리 |
| **회의실 조회** | useRooms | 0% | 완전한 계층 분리 |
| | useRoom | 0% | 완전한 계층 분리 |
| | useBookedSlots | 100% | 직접 RPC 호출, 모든 비즈니스 로직 |
| | useRoomAvailability | 60% | 직접 RPC 호출, 폴백 로직 |
| **데이터 변경** | useCreateReservation | 40% | UI 피드백, 캐시 관리 |
| | useUpdateReservation | 60% | 데이터 변환, UI 피드백, 캐시 관리 |
| | useCancelReservation | 50% | UI 피드백, 캐시 관리 |

### 4.2 전체 아키텍처 건강도

- **평균 서비스 계층 침범도**: 45.5%
- **완전한 계층 분리 달성**: 4개 훅 (36.4%)
- **심각한 침범 (80% 이상)**: 2개 훅 (18.2%)
- **중간 침범 (40-79%)**: 5개 훅 (45.4%)

### 4.3 주요 침범 패턴 분석

#### 🚨 Pattern 1: 직접 RPC 호출 (가장 심각)
```typescript
// 문제 코드
const { data, error } = await supabase.rpc('get_reservations_for_period', {
  start_date: null,
  end_date: null
});
```

**영향받는 훅**: `useMyReservations`, `useBookedSlots`, `useRoomAvailability`

#### 🚨 Pattern 2: 클라이언트 사이드 비즈니스 로직
```typescript
// 문제 코드
const myReservations = (data || []).filter((reservation: any) => reservation.is_mine === true);
const roomReservations = (data || []).filter((reservation: any) =>
  reservation.room_id === roomId && reservation.status !== 'cancelled'
);
```

**영향받는 훅**: `useMyReservations`, `useBookedSlots`

#### 🚨 Pattern 3: 데이터 변환 로직
```typescript
// 문제 코드
const bookedSlots = roomReservations.map((reservation: any) => ({
  id: reservation.id,
  start_time: reservation.start_time,
  end_time: reservation.end_time,
  title: reservation.title,
  user_name: reservation.user_name || '알 수 없음',
  is_mine: reservation.is_mine || false
}));
```

**영향받는 훅**: `useBookedSlots`, `useUpdateReservation`

---

## 🎯 Part 5: 이상적인 4계층 아키텍처와의 Gap 분석

### 5.1 현재 아키텍처 vs 이상적 아키텍처

#### 현재 상태 (혼재된 책임)
```
┌─────────────────┐
│   Presentation  │ ← React Components
├─────────────────┤
│   Hook Layer    │ ← ❌ 서비스 로직 포함 (45.5% 침범)
├─────────────────┤
│   Service Layer │ ← ✅ 일부 구현됨
├─────────────────┤
│   Data Layer    │ ← Supabase RPC/Database
└─────────────────┘
```

#### 이상적 상태 (완전한 계층 분리)
```
┌─────────────────┐
│   Presentation  │ ← React Components
├─────────────────┤
│   Hook Layer    │ ← ✅ 순수 React Query 래퍼
├─────────────────┤
│   Service Layer │ ← ✅ 모든 비즈니스 로직
├─────────────────┤
│   Data Layer    │ ← Supabase RPC/Database
└─────────────────┘
```

### 5.2 주요 Gap 영역

#### Gap 1: 직접 데이터베이스 접근 (심각도: 높음)
- **현재**: 훅에서 `supabase.rpc()` 직접 호출
- **이상**: 서비스 계층을 통한 간접 접근
- **영향**: 테스트 어려움, 비즈니스 로직 분산

#### Gap 2: 비즈니스 로직 분산 (심각도: 높음)
- **현재**: 필터링, 변환 로직이 훅에 분산
- **이상**: 서비스 계층에 집중
- **영향**: 재사용성 저하, 유지보수 어려움

#### Gap 3: 캐시 관리 복잡성 (심각도: 중간)
- **현재**: 훅에서 수동 캐시 조작
- **이상**: 자동화된 캐시 무효화
- **영향**: 데이터 일관성 문제 가능성

---

## 🔧 Part 6: 권장 개선 방안

### 6.1 즉시 조치 (Critical Priority)

#### 1. useMyReservations 리팩토링
```typescript
// Before (80% 침범)
const { data, error } = await supabase.rpc('get_reservations_for_period', {
  start_date: null,
  end_date: null
});
const myReservations = (data || []).filter((reservation: any) => reservation.is_mine === true);

// After (0% 침범)
return reservationService.getMyReservations(supabase, userId);
```

#### 2. useBookedSlots 리팩토링
```typescript
// Before (100% 침범)
const { data, error } = await supabase.rpc('get_reservations_for_period', {
  start_date: startDate,
  end_date: endDate
});
const roomReservations = (data || []).filter(...);
const bookedSlots = roomReservations.map(...);

// After (0% 침범)
return roomService.getBookedSlots(supabase, roomId, date);
```

### 6.2 단기 조치 (High Priority)

#### 3. 서비스 계층 확장
```typescript
// reservationService.ts에 추가
async getMyReservationsOptimized(supabase: SupabaseClient, userId: string) {
  const { data, error } = await supabase.rpc('get_user_reservations_detailed', {
    p_user_id: userId
  });
  
  if (error) throw error;
  return data || [];
}

// roomService.ts에 추가
async getBookedSlots(supabase: SupabaseClient, roomId: string, date: Date) {
  const startDate = format(date, 'yyyy-MM-dd') + 'T00:00:00Z';
  const endDate = format(date, 'yyyy-MM-dd') + 'T23:59:59Z';
  
  const { data, error } = await supabase.rpc('get_reservations_for_period', {
    start_date: startDate,
    end_date: endDate
  });
  
  if (error) throw error;
  
  return (data || [])
    .filter(r => r.room_id === roomId && r.status !== 'cancelled')
    .map(r => ({
      id: r.id,
      start_time: r.start_time,
      end_time: r.end_time,
      title: r.title,
      user_name: r.user_name || '알 수 없음',
      is_mine: r.is_mine || false
    }));
}
```

### 6.3 장기 조치 (Medium Priority)

#### 4. 자동화된 캐시 무효화 시스템
```typescript
// 캐시 무효화 규칙 정의
const cacheInvalidationRules = {
  createReservation: [reservationKeys.all, roomKeys.availability],
  updateReservation: [reservationKeys.all, reservationKeys.my],
  cancelReservation: [reservationKeys.all, roomKeys.availability]
};
```

#### 5. 타입 안전성 강화
```typescript
// 서비스 계층 응답 타입 정의
interface ServiceResponse<T> {
  data: T;
  error?: string;
  metadata?: {
    total: number;
    page: number;
  };
}
```

---

## 🏁 결론 및 권장사항

### 📊 현재 상태 요약
- **전체 아키텍처 건강도**: 54.5% (중간 수준)
- **주요 문제**: 직접 RPC 호출과 비즈니스 로직 분산
- **긍정적 측면**: 36.4%의 훅이 완전한 계층 분리 달성

### 🎯 우선순위별 개선 로드맵

#### Phase 1 (1-2주): 심각한 침범 해결
1. `useMyReservations` 완전 리팩토링
2. `useBookedSlots` 서비스 계층 이관

#### Phase 2 (2-3주): 중간 침범 개선
3. `usePublicReservations` 실시간 로직 분리
4. Mutation 훅들의 UI 로직 분리

#### Phase 3 (3-4주): 시스템 최적화
5. 자동화된 캐시 무효화 구현
6. 타입 안전성 강화

### 🚀 기대 효과
- **테스트 용이성**: 서비스 계층 분리로 단위 테스트 가능
- **재사용성**: 비즈니스 로직의 다른 컨텍스트 재사용
- **유지보수성**: 책임 분리로 코드 변경 영향 최소화
- **성능**: 최적화된 캐시 전략으로 불필요한 리렌더링 방지

**이 개선을 통해 현재 54.5%의 아키텍처 건강도를 90% 이상으로 향상시킬 수 있습니다.**