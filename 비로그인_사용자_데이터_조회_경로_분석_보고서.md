# 비로그인 사용자 데이터 조회 경로 분석 보고서

**TO:** 대표님  
**RE:** `/reservations/status` 페이지의 비로그인 사용자 데이터 조회 정책 검증을 위한 아키텍처 분석 보고서  
**작성일:** 2025년 8월 6일

---

## 📋 목표

`welcome` 페이지의 '전체 예약 현황 보기' 링크를 `/reservations/status`로 변경하기에 앞서, 비로그인 사용자가 해당 페이지에 접근했을 때 **어떤 데이터가, 어떤 규칙에 의해** 보여지는지 완벽하게 파악하고, 우리의 의도(예: 개인정보는 마스킹된 채 예약 현황만 보여주기)와 일치하는지 최종 검증합니다.

---

## 🔍 1. 데이터 조회의 시작점 (프론트엔드)

### 1-1. `/reservations/status` 페이지 컴포넌트 분석

**현재 상태:** ✅ **안전함**

```tsx
// src/app/reservations/status/page.tsx (핵심 부분)
export default function ReservationStatusPage() {
  const { isAuthenticated } = useAuth();
  // ...
  
  const { data: reservations, isLoading, isError } = usePublicReservations(
    startDateStr,
    endDateStr,
    isAuthenticated()  // ← 이 값이 핵심
  );
}
```

**분석 결과:**
- 페이지는 `useAuth()` 훅을 통해 사용자의 인증 상태를 확인
- `isAuthenticated()` 함수의 반환값을 `usePublicReservations` 훅에 전달
- 비로그인 사용자의 경우 `isAuthenticated()`는 `false`를 반환

### 1-2. 공개 예약 조회 훅 분석

**현재 상태:** ⚠️ **문제 발견**

```tsx
// src/hooks/useReservations.ts (핵심 부분)
export function usePublicReservations(startDate: string, endDate: string, isAuthenticated?: boolean, currentUserId?: string) {
  // ...
  const queryResult = useQuery(buildQueryOptions({
    queryKey,
    queryFn: createStandardFetch(
      () => {
        // ✅ [2단계] queryFn 내부에서 최종 방어
        if (authStatus !== 'authenticated') {
          return Promise.resolve([]); // ← 여기가 문제!
        }
        return reservationService.getPublicReservations(startDate, endDate, isAuthenticated);
      },
      // ...
    ),
    enabled: !!startDate && !!endDate && authStatus === 'authenticated', // ← 여기도 문제!
    // ...
  }));
}
```

**🚨 중대한 문제 발견:**
1. **`authStatus !== 'authenticated'` 조건:** 비로그인 사용자는 항상 빈 배열(`[]`)을 받게 됨
2. **`enabled` 조건:** 비로그인 사용자는 쿼리 자체가 실행되지 않음
3. **결과:** 비로그인 사용자가 `/reservations/status`에 접근해도 **아무 데이터도 보이지 않음**

---

## 🔍 2. 데이터 처리의 종착점 (백엔드)

### 2-1. `get_reservations_for_period` RPC 함수 분석

**현재 상태:** ✅ **완벽하게 설계됨**

```sql
-- my_app_backup.sql에서 확인된 실제 함수
CREATE OR REPLACE FUNCTION "public"."get_reservations_for_period"(
  "start_date" timestamp with time zone, 
  "end_date" timestamp with time zone
) RETURNS TABLE(
  "id" "uuid", "room_id" "uuid", "user_id" "uuid", 
  "title" "text", "purpose" "text", 
  "start_time" timestamp with time zone, "end_time" timestamp with time zone, 
  "department" "text", "user_name" "text", "room_name" "text", 
  "is_mine" boolean
)
```

**핵심 데이터 가공 로직:**
```sql
SELECT 
    r.id, r.room_id, r.user_id,
    -- 🔐 제목 마스킹: 본인 예약이 아니면 부서명으로 대체
    CASE WHEN u.auth_id = auth.uid() THEN r.title ELSE u.department END,
    -- 🔐 목적 마스킹: 본인 예약이 아니면 NULL
    CASE WHEN u.auth_id = auth.uid() THEN r.purpose ELSE NULL END,
    r.start_time, r.end_time, u.department,
    -- 🔐 사용자명 마스킹: 본인 예약이 아니면 'Anonymous'
    CASE WHEN u.auth_id = auth.uid() THEN u.name ELSE 'Anonymous' END,
    rm.name as room_name,
    -- 🔐 소유권 플래그: 본인 예약인지 여부
    (u.auth_id = auth.uid())
FROM public.reservations r 
JOIN public.users u ON r.user_id = u.id
JOIN public.rooms rm ON r.room_id = rm.id
WHERE r.status = 'confirmed' 
    AND r.start_time < end_date 
    AND r.end_time > start_date
    AND rm.is_active = true;
```

**비로그인 사용자(`auth.uid()` = `null`)에 대한 동작:**
- **`title`:** 모든 예약이 부서명으로 표시 (예: "개발팀", "마케팅팀")
- **`purpose`:** 모든 예약이 `NULL`로 표시
- **`user_name`:** 모든 예약이 `'Anonymous'`로 표시
- **`is_mine`:** 모든 예약이 `false`로 표시
- **권한:** `anon` 역할에게도 실행 권한이 부여됨 (`GRANT ALL ON FUNCTION ... TO "anon";`)

---

## 🔍 3. 현재 `welcome/page.tsx`의 문제점

**현재 링크:** `/kiosk/room-display` ❌  
**올바른 링크:** `/reservations/status` ✅

```tsx
// src/app/welcome/page.tsx (문제 부분)
<Anchor 
  component={Link} 
  href="/kiosk/room-display"  // ← 잘못된 경로
  c="blue" 
  size="sm"
  fw={500}
>
  <Group gap="xs" justify="center">
    <Clock size={14} />
    <Text>전체 예약 현황 둘러보기</Text>
    <ArrowRight size={14} />
  </Group>
</Anchor>
```

---

## 📊 4. 종합 분석 결과

### 4-1. 백엔드 (RPC 함수): ✅ 완벽
- 비로그인 사용자에게 적절히 마스킹된 데이터 제공
- 개인정보 보호 완벽 구현
- 예약 현황은 부서명으로 표시되어 유용한 정보 제공

### 4-2. 프론트엔드 (React 훅): ❌ 치명적 문제
- 비로그인 사용자는 데이터를 전혀 받을 수 없음
- 백엔드의 완벽한 마스킹 로직이 무용지물

### 4-3. 라우팅: ❌ 단순 오타
- `welcome` 페이지의 링크가 잘못된 경로를 가리킴

---

## 🛠️ 5. 해결 방안

### 5-1. 즉시 해결 (링크 수정)
```tsx
// src/app/welcome/page.tsx
<Anchor 
  component={Link} 
  href="/reservations/status"  // ✅ 올바른 경로로 수정
  // ...
>
```

### 5-2. 근본 해결 (훅 수정)
```tsx
// src/hooks/useReservations.ts
export function usePublicReservations(startDate: string, endDate: string, isAuthenticated?: boolean, currentUserId?: string) {
  // ...
  const queryResult = useQuery(buildQueryOptions({
    queryKey,
    queryFn: createStandardFetch(
      () => {
        // ✅ 비로그인 사용자도 데이터 조회 허용
        return reservationService.getPublicReservations(startDate, endDate, isAuthenticated);
      },
      // ...
    ),
    // ✅ 인증 상태와 관계없이 쿼리 실행 허용
    enabled: !!startDate && !!endDate,
    // ...
  }));
}
```

---

## 🎯 6. 최종 권고사항

1. **즉시 실행:** `welcome/page.tsx`의 링크를 `/reservations/status`로 수정
2. **우선 실행:** `usePublicReservations` 훅에서 비로그인 사용자 차단 로직 제거
3. **테스트 필요:** 수정 후 비로그인 상태에서 실제 데이터가 마스킹되어 표시되는지 확인

**예상 결과:**
- 비로그인 사용자가 "전체 예약 현황 둘러보기"를 클릭하면
- `/reservations/status` 페이지로 이동하여
- 모든 예약이 부서명으로 표시되고 (예: "개발팀", "마케팅팀")
- 사용자명은 "Anonymous"로 표시되며
- 드래그 앤 드롭 등 수정 기능은 비활성화됨

이로써 **개인정보는 완벽히 보호하면서도, 전체 예약 현황은 유용하게 제공**하는 목표를 달성할 수 있습니다.