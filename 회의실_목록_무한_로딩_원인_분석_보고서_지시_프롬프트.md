# 회의실 목록 무한 로딩 현상 원인 분석 보고서 지시 프롬프트

## 📋 분석 목표

'새 예약'(`reservations/new`) 페이지에서 발생하는 **회의실 목록 무한 로딩** 문제의 근본 원인을 데이터 기반으로 명확히 특정합니다. 

특히 'New Standard' 원칙 중 **'예측 가능한 데이터 흐름'**이 아키텍처의 어느 부분에서 깨졌는지 실제 데이터로 증명하는 것을 목표로 합니다.

---

## 🔍 필수 제공 정보 (Markdown 형식)

### 1. 핵심 문제 지점: `useQuery` 실행부 전체 코드

**요청사항:**
- '새 예약' 페이지 또는 그 하위 컴포넌트에서 **회의실 목록을 조회하는 `useQuery` 훅**이 포함된 React 컴포넌트의 **전체 소스 코드**
- `useGetMeetingRooms`와 같은 커스텀 훅을 사용한다면, **해당 커스텀 훅의 소스 코드**도 반드시 포함
- 파일 경로와 함께 제공

**제공 형식:**
```markdown
#### 파일 경로: `src/components/reservations/MeetingRoomSelector.tsx`
```tsx
// 전체 소스 코드
```

---

### 2. 렌더링 시점의 인증 상태 추적 로그

**요청사항:**
- 위 1번 항목의 컴포넌트 내부에 다음 디버깅 코드를 추가:

```tsx
import { useAuth } from '@/providers/auth-provider';
import { useQuery } from '@tanstack/react-query';

// ... 컴포넌트 내부 ...
const { authStatus } = useAuth();
const { status, isFetching, isEnabled, data } = useQuery({
  queryKey: ['meetingRooms'],
  queryFn: getMeetingRooms,
  enabled: authStatus === 'authenticated',
});

console.log(`[MeetingRoomLoader] Render - Auth: ${authStatus}, Query Status: ${status}, isFetching: ${isFetching}, isEnabled: ${isEnabled}, Data: ${data ? 'exists' : 'null'}`);
```

- 페이지에 처음 진입했을 때 **콘솔에 출력되는 모든 로그**를 시간 순서대로 복사

**제공 형식:**
```markdown
#### 콘솔 로그 (시간 순서)
```
[MeetingRoomLoader] Render - Auth: loading, Query Status: idle, isFetching: false, isEnabled: false, Data: null
[MeetingRoomLoader] Render - Auth: authenticated, Query Status: loading, isFetching: true, isEnabled: true, Data: null
...
```
```

---

### 3. React Query Devtools 상태 스크린샷

**요청사항:**
- 문제 발생 상황에서 **React Query Devtools** 열기
- 회의실 목록 조회 `queryKey` (예: `['meetingRooms']`) 항목의 **상세 정보 스크린샷**
- Query Status, Data, Query Observer 등이 모두 보이도록 캡처

**제공 형식:**
```markdown
#### React Query Devtools 스크린샷
![React Query Devtools](스크린샷_파일명.png)

**주요 정보:**
- Query Key: ['meetingRooms']
- Status: loading/success/error
- Data: 존재 여부
- Last Updated: 시간
```

---

### 4. 브라우저 네트워크 탭 분석

**요청사항:**
- 개발자 도구 '네트워크' 탭에서 페이지 진입 시 상황 재현
- 회의실 목록을 가져오는 **Supabase RPC 호출** 확인

**A. 네트워크 요청이 발생한 경우:**
```markdown
#### 네트워크 요청 분석
**요청 URL:** `https://your-project.supabase.co/rest/v1/rpc/get_meeting_rooms`

**Headers:**
```
Authorization: Bearer eyJ...
Content-Type: application/json
...
```

**Payload:**
```json
{
  "param1": "value1"
}
```

**Response:**
```json
{
  "data": [...],
  "status": 200
}
```
```

**B. 네트워크 요청이 발생하지 않은 경우:**
```markdown
#### 네트워크 요청 분석
**결과:** 회의실 목록 조회를 위한 네트워크 요청이 **발생하지 않음**

**의미:** `useQuery`가 `enabled: false` 상태로 멈춰있을 가능성이 높음
```

---

### 5. (선택사항) Supabase 대시보드 API 로그

**요청사항:**
- Supabase 프로젝트 대시보드 → `Logs` → `API Logs`
- 문제 재현 시점의 관련 로그 확인

**제공 형식:**
```markdown
#### Supabase API 로그
**시간:** 2025-01-XX XX:XX:XX
**요청:** POST /rest/v1/rpc/get_meeting_rooms
**상태:** 200 OK / 400 Bad Request / 500 Internal Server Error
**응답 시간:** XXXms

**로그 내용:**
```
로그 상세 내용...
```
```

---

## 📊 분석 결과 활용 방안

위 5가지 정보를 정확히 제공해주시면:

1. **추측 배제**: 실제 데이터에 기반한 정확한 원인 특정
2. **아키텍처 진단**: 'New Standard' 원칙 위반 지점 명확화
3. **최적 해결책**: 근본 원인에 맞는 정확한 수정 방안 제시
4. **재발 방지**: 동일한 패턴의 문제 예방 가이드라인 수립

---

## ⚠️ 중요 참고사항

- 모든 정보는 **문제 재현 과정에서 수집된 실제 데이터**여야 합니다
- 추측이나 가정이 아닌 **관찰된 사실**만 기록해주십시오
- 스크린샷은 가능한 한 **고해상도**로 제공해주십시오
- 민감한 정보(토큰, 개인정보 등)는 **마스킹 처리** 후 제공해주십시오

---

**이 지시 프롬프트를 사용하여 필요한 정보를 수집하신 후, 해당 데이터를 바탕으로 정확한 원인 분석 보고서를 요청해주십시오.**