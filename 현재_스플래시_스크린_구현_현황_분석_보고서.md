# 현재 스플래시 스크린 구현 현황 분석 보고서

**TO:** 대표님  
**RE:** 현재 애플리케이션의 스플래시 스크린(초기 로딩 화면) 구현 현황 파악을 위한 종합 분석 보고서  
**작성일:** 2025년 8월 6일

---

## 📋 목표

"앱 시작 시 로고가 애니메이션과 함께 나오는" 전문적인 스플래시 스크린 기능을 구현하기에 앞서, 현재 프로젝트에 이미 구현되어 있거나 관련이 있는 모든 코드와 설정을 파악하여, 기존 로직과의 충돌을 피하고 가장 효율적인 통합/개선 방안을 수립합니다.

---

## 1. PWA 레벨의 스플래시 스크린 설정 (OS가 보여주는 화면)

### 1-1. `manifest.json` 현황

**파일 위치:** `public/manifest.json`

```json
{
  "name": "Easyroom 회의실 예약",
  "short_name": "Easyroom",
  "description": "간편한 회의실 예약 시스템",
  "start_url": "/",
  "display": "standalone",
  "scope": "/",
  "background_color": "#ffffff",
  "theme_color": "#2563eb",
  "icons": [
    { "src": "/icons/icon-72x72.png", "sizes": "72x72", "type": "image/png" },
    { "src": "/icons/icon-96x96.png", "sizes": "96x96", "type": "image/png" },
    { "src": "/icons/icon-128x128.png", "sizes": "128x128", "type": "image/png" },
    { "src": "/icons/icon-144x144.png", "sizes": "144x144", "type": "image/png" },
    { "src": "/icons/icon-152x152.png", "sizes": "152x152", "type": "image/png" },
    {
      "src": "/icons/icon-192x192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    { "src": "/icons/icon-384x384.png", "sizes": "384x384", "type": "image/png" },
    { "src": "/icons/icon-512x512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

**📊 분석 결과:**
- ✅ **기본 PWA 스플래시 스크린 설정 완료**
- ✅ 배경색: 흰색 (`#ffffff`)
- ✅ 테마색: 파란색 (`#2563eb`)
- ✅ 아이콘 세트 완비 (72x72 ~ 512x512)
- ✅ `maskable` 아이콘 지원

### 1-2. `layout.tsx` 메타데이터 설정

**파일 위치:** `src/app/layout.tsx`

```typescript
export const metadata: Metadata = {
  title: '회의실 예약 시스템',
  description: '간편한 회의실 예약 시스템',
  manifest: '/manifest.json',
  icons: {
    icon: [
      { url: '/icons/icon-72x72.png', sizes: '72x72', type: 'image/png' },
      { url: '/icons/icon-96x96.png', sizes: '96x96', type: 'image/png' },
      { url: '/icons/icon-128x128.png', sizes: '128x128', type: 'image/png' },
      { url: '/icons/icon-144x144.png', sizes: '144x144', type: 'image/png' },
      { url: '/icons/icon-152x152.png', sizes: '152x152', type: 'image/png' },
      { url: '/icons/icon-192x192.png', sizes: '192x192', type: 'image/png' },
      { url: '/icons/icon-384x384.png', sizes: '384x384', type: 'image/png' },
      { url: '/icons/icon-512x512.png', sizes: '512x512', type: 'image/png' },
    ],
    apple: [
      { url: '/icons/icon-152x152.png', sizes: '152x152', type: 'image/png' },
      { url: '/icons/icon-192x192.png', sizes: '192x192', type: 'image/png' },
    ],
  },
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '회의실 예약',
  },
  applicationName: '회의실 예약 시스템',
};
```

**📊 분석 결과:**
- ✅ **iOS Safari 스플래시 스크린 설정 완료**
- ✅ Apple Web App 메타태그 설정
- ✅ 다양한 해상도 아이콘 지원
- ✅ PWA manifest 연결

---

## 2. 애플리케이션 레벨의 로딩 UI (React가 보여주는 화면)

### 2-1. `AuthGatekeeper`의 로딩 화면

**파일 위치:** `src/components/layout/AuthGatekeeper.tsx`

```typescript
function FullScreenLoader() {
    return (
        <div className="flex items-center justify-center min-h-screen bg-gray-50">
            <div className="text-center">
                <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
                <p className="text-sm text-gray-600">인증 확인 중...</p>
            </div>
        </div>
    );
}

export default function AuthGatekeeper({ children }: AuthGatekeeperProps) {
    const { authStatus } = useAuthContext();

    if (authStatus === 'loading') {
        return <FullScreenLoader />;
    }

    return <>{children}</>;
}
```

**📊 분석 결과:**
- ✅ **인증 상태 확인 중 로딩 화면 존재**
- 🎨 디자인: 회전하는 스피너 + "인증 확인 중..." 텍스트
- 🎨 배경: 연한 회색 (`bg-gray-50`)
- 🎨 스피너: 파란색 테두리 (`border-blue-600`)

### 2-2. 루트 페이지의 로딩 화면

**파일 위치:** `src/app/page.tsx`

```typescript
export default function HomePage() {
  const { authStatus } = useAuthContext();
  const router = useRouter();

  useEffect(() => {
    if (authStatus === 'unauthenticated') {
      router.replace('/welcome');
    } else if (authStatus === 'authenticated') {
      router.replace('/dashboard');
    }
  }, [authStatus, router]);

  return (
    <Center mih="100vh">
      <div className="text-center">
        <Loader size="lg" />
        <Text size="lg" fw={600} mt="md">EasyRoom</Text>
        <Text size="sm" c="dimmed" mt="xs">
          {authStatus === 'loading' 
            ? '인증 상태를 확인하고 있습니다...' 
            : '적절한 페이지로 이동하고 있습니다...'
          }
        </Text>
      </div>
    </Center>
  );
}
```

**📊 분석 결과:**
- ✅ **라우팅 허브 역할의 로딩 화면 존재**
- 🎨 디자인: Mantine Loader + "EasyRoom" 브랜드명
- 🎨 상태별 메시지 표시
- 🎨 중앙 정렬 레이아웃

---

## 3. 앱의 진입점 및 구조 (스플래시 스크린이 삽입될 위치)

### 3-1. `RootLayout` 구조

**파일 위치:** `src/app/layout.tsx`

```typescript
export default async function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const supabase = await createClient();
  const { data: { session } } = await supabase.auth.getSession();
  
  return (
    <html lang="ko" suppressHydrationWarning>
      <body className={`${inter.className} min-h-screen bg-background antialiased`}>
        <ClientPolyfillManager enableServiceWorker={true} enablePWAComponents={true}>
          <Providers>
            <SupabaseProvider>
              <AuthProvider initialSession={session}>
                <AuthGatekeeper>
                  {children}
                </AuthGatekeeper>
                <AuthToastManager />
                <Toaster />
                <UpdateNotification />
                <GlobalNotification />
              </AuthProvider>
            </SupabaseProvider>
          </Providers>
        </ClientPolyfillManager>
      </body>
    </html>
  );
}
```

**📊 분석 결과:**
- ✅ **계층적 프로바이더 구조 확인**
- 🔄 실행 순서: `ClientPolyfillManager` → `Providers` → `SupabaseProvider` → `AuthProvider` → `AuthGatekeeper`
- 🎯 **스플래시 스크린 삽입 최적 위치**: `AuthProvider` 내부, `AuthGatekeeper` 이전

---

## 🎯 종합 분석 및 권장사항

### 현재 시스템의 로딩 화면 현황

1. **PWA 레벨 (OS 제공)**
   - ✅ 기본 스플래시 스크린 설정 완료
   - 🎨 흰색 배경 + 파란색 테마 + 앱 아이콘

2. **React 레벨 (앱 제공)**
   - ✅ `AuthGatekeeper`: 인증 확인 중 로딩 화면
   - ✅ `HomePage`: 라우팅 허브 로딩 화면

### 새로운 애니메이션 스플래시 스크린 통합 전략

#### 🎯 **권장 삽입 위치**
```typescript
<AuthProvider initialSession={session}>
  {/* 🆕 새로운 애니메이션 스플래시 스크린 삽입 지점 */}
  <SplashScreen>
    <AuthGatekeeper>
      {children}
    </AuthGatekeeper>
  </SplashScreen>
</AuthProvider>
```

#### 🔄 **로딩 화면 실행 순서**
1. **PWA 스플래시** (OS 제공) → 앱 로딩 완료
2. **애니메이션 스플래시** (새로 구현) → 브랜드 경험 제공
3. **AuthGatekeeper 로딩** (기존) → 인증 상태 확인
4. **HomePage 로딩** (기존) → 라우팅 처리

#### ⚠️ **충돌 방지 방안**
- 새로운 스플래시 스크린은 기존 로딩 화면을 **대체하지 않고 보완**
- 각 로딩 화면은 **서로 다른 단계**에서 실행되어 자연스러운 전환 제공
- 기존 `FullScreenLoader`와 `HomePage` 로딩 UI는 **그대로 유지**

### 🎨 **디자인 일관성 권장사항**
- **색상 팔레트**: 기존 파란색 테마 (`#2563eb`) 활용
- **브랜드 요소**: "EasyRoom" 로고 + 애니메이션
- **전환 효과**: 부드러운 페이드 인/아웃
- **지속 시간**: 2-3초 (너무 길지 않게)

---

## 📝 결론

현재 시스템은 **체계적인 로딩 화면 구조**를 갖추고 있으며, 새로운 애니메이션 스플래시 스크린을 추가하기에 **최적의 환경**입니다. 기존 로직과의 충돌 없이 **AuthProvider 내부**에 새로운 컴포넌트를 삽입하여, 사용자에게 **일관되고 전문적인 브랜드 경험**을 제공할 수 있습니다.

**다음 단계**: 애니메이션 스플래시 스크린 컴포넌트 설계 및 구현 진행