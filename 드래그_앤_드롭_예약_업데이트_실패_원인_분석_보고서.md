# 드래그 앤 드롭 예약 업데이트 실패 원인 분석 보고서

**작성일:** 2025-08-04  
**작성자:** AI 개발팀  
**대상:** 프로젝트 리드  
**목적:** 드래그 앤 드롭 후 예약 원위치 현상의 근본 원인 규명

---

## 🔍 현상 요약

사용자가 드래그 앤 드롭으로 예약 시간을 변경할 때, UI상에서는 정상적으로 동작하지만 실제 데이터베이스에는 변경사항이 반영되지 않아 예약이 원래 위치로 되돌아가는 현상이 발생하고 있습니다.

---

## 🕵️ 데이터 여정 추적 분석

### 관문 ① : `useUpdateReservation` 훅 분석

**파일:** `src/hooks/useReservations.ts`

```typescript
export function useUpdateReservation() {
  const queryClient = useQueryClient();
  const supabase = useSupabaseClient();

  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: Partial<ReservationFormData> }) => {
      if (!supabase) {
        throw new Error('인증 컨텍스트를 사용할 수 없어 예약을 수정할 수 없습니다.');
      }
      
      // Transform Date objects to ISO strings for database
      const updateData: Partial<ReservationUpdate> = {};
      if (data.title) {
        updateData.title = data.title;
      }
      if (data.purpose) {
        updateData.purpose = data.purpose;
      }
      if (data.start_time) {
        updateData.start_time = data.start_time.toISOString();
      }
      if (data.end_time) {
        updateData.end_time = data.end_time.toISOString();
      }
      
      return reservationService.updateReservation(supabase, id, updateData);
    },
    onSuccess: (updatedReservation) => {
      queryClient.invalidateQueries({ 
        queryKey: reservationKeys.all,
        exact: false
      });
      queryClient.invalidateQueries({
        queryKey: reservationKeys.detail(updatedReservation.id)
      });
      toast.success('예약이 수정되었습니다.');
    },
    onError: (error) => {
      toast.error('예약 수정 실패', {
        description: error instanceof Error ? error.message : '예약 수정 중 오류가 발생했습니다.',
      });
    },
  });
}
```

**분석 결과:**
- ✅ 데이터 변환 로직 정상 (Date → ISO string)
- ✅ 에러 핸들링 구현됨
- ⚠️ **캐시 무효화 키 불일치 의심**: `reservationKeys.all`을 무효화하지만, 실제 캘린더에서 사용하는 키와 다를 수 있음

---

### 관문 ② : `reservationService.updateReservation` 분석

**파일:** `src/lib/services/reservations.ts`

```typescript
async updateReservation(supabase: SupabaseClient<Database>, id: string, data: ReservationUpdate): Promise<Reservation> {
  try {
    const validatedData = await UserIdGuards.validateReservationUpdateData(supabase, data);
    const { data: reservation, error } = await supabase
      .from('reservations')
      .update(validatedData)
      .eq('id', id)
      .select(`*, room:rooms!inner(*)`) // ✅ 관계형 조회 구문 통일
      .single(); // ✅ .single()로 단일 객체 반환 보장

    if (error) throw error;
    if (!reservation) throw new Error('예약을 수정하고 데이터를 가져오는 데 실패했습니다.');

    return reservation as Reservation;
  } catch (error) {
    logger.error('예약 수정 실패', { error });
    throw new Error('예약을 수정하는데 실패했습니다.');
  }
}
```

**분석 결과:**
- ✅ Supabase 쿼리 구문 정상
- ✅ 에러 핸들링 구현됨
- ⚠️ **UserIdGuards.validateReservationUpdateData 의존성**: 이 함수가 데이터를 변조하거나 검증 실패할 가능성

---

### 관문 ③ : Supabase RLS 정책 분석 (🚨 **핵심 용의자**)

**테이블:** `public.reservations`  
**정책:** `reservations_update_own`

```sql
CREATE POLICY "reservations_update_own" ON "public"."reservations" 
FOR UPDATE USING (("user_id" IN ( 
  SELECT "users"."id"
  FROM "public"."users"
  WHERE ("users"."auth_id" = "auth"."uid"())
)));
```

**분석 결과:**
- 🚨 **치명적 발견**: 이 정책은 **오직 예약자 본인만** 예약을 수정할 수 있도록 제한
- 🚨 **권한 검증 실패 시 조용한 실패**: Supabase는 권한이 없을 때 에러를 던지지 않고 **0개 행 업데이트**로 처리
- 🚨 **드래그 앤 드롭 컨텍스트 문제**: 다른 사용자의 예약을 드래그할 때 권한 부족으로 업데이트 실패

---

### 관문 ④ : 캐시 무효화 키 불일치 분석

**현재 무효화 키:**
```typescript
queryClient.invalidateQueries({ 
  queryKey: reservationKeys.all,  // ['reservations']
  exact: false
});
```

**실제 캘린더에서 사용하는 키:**
```typescript
// GoogleCalendarView에서 사용하는 데이터 소스
const queryKey = reservationKeys.public(startDate, endDate, isAuthenticated);
// 실제 키: ['reservations', 'public', startDate, endDate, 'auth', isAuthenticated]
```

**분석 결과:**
- ⚠️ **부분적 불일치**: `reservationKeys.all`은 `['reservations']`이므로 `exact: false` 옵션으로 하위 키들도 무효화되어야 함
- ✅ 이론적으로는 캐시 무효화가 작동해야 함

---

## 🎯 최종 진단 및 범인 지목

### 🚨 **주범: Supabase RLS 정책 권한 부족**

**근본 원인:**
1. **다른 사용자의 예약을 드래그**할 때 `reservations_update_own` 정책에 의해 업데이트가 **조용히 실패**
2. Supabase는 권한 부족 시 에러를 던지지 않고 `{ data: null, error: null, count: 0 }`을 반환
3. `reservationService.updateReservation`에서 `error`가 없으므로 성공으로 간주
4. 실제로는 데이터베이스에 변경사항이 반영되지 않음
5. 캐시 무효화 후 서버에서 다시 데이터를 가져올 때 원래 상태로 복원

### 🔧 **해결 방안**

#### 방안 1: 권한 검증 강화 (권장)
```typescript
// handleDragEnd 함수에서 드래그 전 권한 확인
const reservation = reservations.find(r => r.id === reservationId);
if (!reservation?.is_mine) {
  toast.error('본인의 예약만 수정할 수 있습니다.');
  return;
}
```

#### 방안 2: 서비스 레벨 검증 강화
```typescript
// reservationService.updateReservation에서 업데이트 결과 확인
const { data: reservation, error, count } = await supabase
  .from('reservations')
  .update(validatedData)
  .eq('id', id)
  .select(`*, room:rooms!inner(*)`)
  .single();

if (error) throw error;
if (count === 0) throw new Error('권한이 없거나 존재하지 않는 예약입니다.');
```

#### 방안 3: RLS 정책 수정 (신중 검토 필요)
```sql
-- 관리자는 모든 예약 수정 가능하도록 정책 확장
CREATE POLICY "reservations_update_own_or_admin" ON "public"."reservations" 
FOR UPDATE USING ((
  "user_id" IN (SELECT "users"."id" FROM "public"."users" WHERE ("users"."auth_id" = "auth"."uid"()))
  OR 
  EXISTS (SELECT 1 FROM "public"."users" WHERE "users"."auth_id" = "auth"."uid"() AND "users"."role" = 'admin')
));
```

---

## 📋 권장 조치사항

1. **즉시 조치**: 방안 1을 적용하여 UI에서 권한 검증
2. **단기 조치**: 방안 2를 적용하여 서비스 레벨 검증 강화  
3. **장기 검토**: 비즈니스 요구사항에 따라 RLS 정책 수정 검토

이 분석을 통해 드래그 앤 드롭 예약 업데이트 실패의 근본 원인이 **Supabase RLS 정책의 권한 제한**임을 확인했습니다.