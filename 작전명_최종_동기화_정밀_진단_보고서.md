# [작전명: 최종 동기화 (Operation: Final Sync)] - 정밀 진단 보고서

**TO:** 프로젝트 책임자  
**FROM:** 기술 분석팀 (Kiro AI)  
**DATE:** 2025년 8월 5일  
**SUBJECT:** RPC 개편 후 '내 예약' 기능 불안정 현상에 대한 근본 원인 분석

---

## 📋 분석 목표

RPC(Remote Procedure Call) 최적화 이후 발생한 '내 예약' 페이지의 데이터 조회 실패 현상을 분석한다. 'detailed' RPC와 'period' RPC 간의 데이터 흐름 불일치 지점을 특정하고, 이를 우리가 수립한 **'아키텍처 통합' 원칙**에 맞춰 재구성함으로써, 시스템 전체의 인증 및 데이터 로딩 안정성을 완벽하게 확보하는 것을 목표로 한다.

---

## 🔍 Part 1: 문제의 진원지 - '내 예약' 관련 코드 분석

### 1.1 내 예약 페이지 컴포넌트 (`src/app/reservations/my/page.tsx`)

```typescript
export default function MyReservationsPage() {
  const router = useRouter();

  return (
    <div className="min-h-screen bg-background">
      <MobileHeader title="내 예약 관리" />
      <main className="container mx-auto p-4 py-8">
        {/* ✅ Props 없이 호출합니다. 이제 이 컴포넌트가 모든 것을 알아서 처리합니다. */}
        <ReservationListView />
      </main>
    </div>
  );
}
```

**분석 결과:**
- ✅ 단순한 래퍼 컴포넌트로 문제 없음
- ✅ `ReservationListView`에 모든 로직을 위임하는 깔끔한 구조

### 1.2 ReservationListView 컴포넌트 (`src/features/reservation/components/ReservationListView.tsx`)

```typescript
export function ReservationListView() {
  const router = useRouter();
  const [cancelingReservation, setCancelingReservation] = useState<ReservationWithDetails | null>(null);
  
  // ✅ Phase 2: 컴포넌트가 userProfile에서 userId를 추출하여 훅에 주입
  const { userProfile } = useAuth();
  const { data: reservations = [], isLoading, isError } = useMyReservations(userProfile?.dbId);

  if (isLoading) {
    return <ReservationListSkeleton />;
  }

  if (isError) {
    return (
      <Card><CardContent className="text-center py-6 text-destructive">예약 목록을 불러오는데 실패했습니다.</CardContent></Card>
    );
  }

  if (reservations.length === 0) {
    return (
      <Card>
        <CardContent className="text-center py-8">
          <Calendar className="mx-auto h-12 w-12 text-muted-foreground mb-4" />
          <h3 className="text-lg font-semibold">예약이 없습니다</h3>
          <p className="text-muted-foreground">새로운 회의실을 예약해보세요.</p>
          <Button className="mt-4" onClick={() => router.push('/reservations/new')}>새 예약하기</Button>
        </CardContent>
      </Card>
    );
  }
  // ... 예약 목록 렌더링
}
```

**🚨 핵심 발견:**
- ✅ `userProfile?.dbId`를 `useMyReservations` 훅에 정확히 전달
- ✅ 로딩, 에러, 빈 상태 처리 완벽 구현
- ⚠️ **"예약이 없습니다" 메시지가 표시되는 지점**: `reservations.length === 0` 조건

### 1.3 useMyReservations 훅 (`src/hooks/useReservations.ts`)

```typescript
export function useMyReservations(userId: string | undefined): { data: ReservationWithDetails[] | undefined; isLoading: boolean; isError: boolean; error: any } {
  const { authStatus } = useAuthContext();
  const supabase = useSupabaseClient();

  const queryOptions = buildQueryOptions({
    queryKey: reservationKeys.my(userId),
    queryFn: createStandardFetch(
      async () => {
        if (authStatus !== 'authenticated' || !userId || !supabase) {
          return Promise.resolve([]);
        }
        
        // ✅ [레거시 RPC 함수 대체] get_reservations_for_period 통합 함수 사용
        const { data, error } = await supabase.rpc('get_reservations_for_period', {
          start_date: null, // 전체 기간 조회
          end_date: null
        });

        if (error) {
          logger.error('get_reservations_for_period RPC failed', error);
          throw new Error(`내 예약 조회 실패: ${error.message}`);
        }

        // ✅ [클라이언트 사이드 필터링] is_mine === true인 예약들만 필터링
        const myReservations = (data || []).filter((reservation: any) => reservation.is_mine === true);
        
        logger.debug('내 예약 조회 완료', { 
          totalReservations: data?.length || 0,
          myReservations: myReservations.length 
        });

        return myReservations as ReservationWithDetails[];
      },
      { operation: 'fetch my reservations (unified RPC)', params: { userId } }
    ),
    // ✅ [Phase 2] DI 패턴 적용 - userId 인자 의존성
    enabled: authStatus === 'authenticated' && !!userId && !!supabase,
    dataType: 'semi-static',
    cacheConfig: {
      customStaleTime: 0,
      customGcTime: 5 * 60 * 1000,
    }
  });

  return useQuery({
    ...queryOptions,
    refetchOnMount: 'always',
    refetchOnWindowFocus: true,
    refetchOnReconnect: true,
  });
}
```

**🚨 중대한 발견 - 데이터 흐름의 핵심 불일치:**
1. **RPC 함수 불일치**: `get_reservations_for_period` 사용 중이지만, 실제로는 `get_user_reservations_detailed` 함수가 필요
2. **클라이언트 사이드 필터링 의존**: `is_mine === true` 필터링에 의존하고 있으나, 이는 `get_reservations_for_period`의 반환 구조
3. **타입 불일치**: `ReservationWithDetails[]`를 기대하지만 실제 반환되는 데이터 구조가 다를 수 있음

---

## 🔧 Part 2: 새로운 데이터 소스 - RPC 함수 정의 분석

### 2.1 내 예약 전용 'detailed' RPC 함수 (제공된 최신 버전)

```sql
CREATE OR REPLACE FUNCTION get_user_reservations_detailed(
    p_user_id UUID
)
RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER
AS $function$
DECLARE
    reservation_data jsonb;
BEGIN
    -- 권한 검사는 그대로 유지
    IF NOT ((SELECT id FROM users WHERE auth_id = auth.uid()) = p_user_id OR 
            EXISTS (SELECT 1 FROM users WHERE auth_id = auth.uid() AND role = 'admin')) THEN
        RAISE EXCEPTION 'Access Denied';
    END IF;

    -- 예약 데이터를 JSON 배열로 만듭니다.
    SELECT jsonb_agg(
        jsonb_build_object(
            'id', r.id,
            'room_id', r.room_id,
            'user_id', r.user_id,
            'title', r.title,
            'purpose', r.purpose,
            'start_time', r.start_time,
            'end_time', r.end_time,
            'status', r.status,
            'created_at', r.created_at,
            'updated_at', r.updated_at,
            'room', to_jsonb(rm),
            'user', to_jsonb(u)
        )
        -- ✅ [핵심 수정] DESC (내림차순)를 ASC (오름차순)으로 변경합니다.
        ORDER BY r.start_time ASC
    )
    INTO reservation_data
    FROM reservations r
    JOIN rooms rm ON r.room_id = rm.id
    JOIN users u ON r.user_id = u.id
    WHERE r.user_id = p_user_id;

    -- 결과가 없으면 빈 JSON 배열을 반환합니다.
    RETURN COALESCE(reservation_data, '[]'::jsonb);
END;
$function$;
```

**분석 결과:**
- ✅ **SECURITY DEFINER**: 적절한 보안 컨텍스트
- ✅ **권한 검증**: 본인 또는 관리자만 조회 가능
- ✅ **완전한 데이터 구조**: `room`, `user` 객체 포함된 `ReservationWithDetails` 구조
- ✅ **정렬 순서**: ASC로 시간순 정렬
- ✅ **빈 결과 처리**: `COALESCE`로 빈 배열 반환 보장

### 2.2 'period' 기반 RPC 함수 (현재 사용 중인 함수)

```sql
CREATE OR REPLACE FUNCTION public.get_reservations_for_period(start_date timestamptz, end_date timestamptz)
RETURNS TABLE ( id uuid, room_id uuid, user_id uuid, title text, purpose text, start_time timestamptz, end_time timestamptz, department text, user_name text, is_mine boolean )
LANGUAGE plpgsql SECURITY INVOKER SET search_path = public AS $$
BEGIN
    RETURN QUERY
    SELECT r.id, r.room_id, r.user_id,
        CASE WHEN u.auth_id = auth.uid() THEN r.title ELSE u.department END,
        CASE WHEN u.auth_id = auth.uid() THEN r.purpose ELSE NULL END,
        r.start_time, r.end_time, u.department,
        CASE WHEN u.auth_id = auth.uid() THEN u.name ELSE 'Anonymous' END,
        (u.auth_id = auth.uid())
    FROM public.reservations r JOIN public.users u ON r.user_id = u.id
    WHERE r.status = 'confirmed' AND r.start_time < end_date AND r.end_time > start_date;
END; $$;
```

**🚨 핵심 불일치 발견:**
1. **반환 구조 완전히 다름**: 
   - `get_reservations_for_period`: 플랫 테이블 구조 + `is_mine` 플래그
   - `get_user_reservations_detailed`: 중첩된 JSON 구조 + 완전한 관계 데이터
2. **보안 컨텍스트 차이**: 
   - `SECURITY INVOKER` vs `SECURITY DEFINER`
3. **데이터 필터링 방식**:
   - Period: 서버사이드에서 `is_mine` 계산
   - Detailed: 클라이언트가 `user_id` 직접 전달

---

## 🌉 Part 3: 데이터 흐름의 다리 - 서비스 계층 및 타입 정의

### 3.1 데이터 서비스 파일 (`src/lib/services/reservations.ts`)

```typescript
/**
 * '내 예약' 목록을 최적화된 방식으로 조회합니다.
 * 통합 RPC 함수를 사용하여 클라이언트 사이드에서 필터링합니다.
 */
async getMyReservationsOptimized(supabase: SupabaseClient<Database>, userId: string): Promise<ReservationWithDetails[]> {
  if (!userId) {
    logger.warn('사용자 ID가 없어 최적화된 예약 조회를 할 수 없습니다');
    return [];
  }

  try {
    // ✅ [레거시 RPC 함수 대체] get_reservations_for_period 통합 함수 사용
    const { data, error } = await supabase.rpc('get_reservations_for_period', {
      start_date: null, // 전체 기간 조회
      end_date: null
    });

    if (error) {
      logger.error('get_reservations_for_period RPC failed', error);
      throw new Error(`내 예약 조회 실패: ${error.message}`);
    }

    // ✅ [클라이언트 사이드 필터링] is_mine === true인 예약들만 필터링
    const myReservations = (data || []).filter((reservation: any) => reservation.is_mine === true);
    
    logger.info('Successfully fetched my reservations via unified RPC', {
      totalReservations: data?.length || 0,
      myReservations: myReservations.length
    });

    return myReservations as ReservationWithDetails[];
  } catch (error) {
    logger.error('내 예약 목록 조회 실패', { error });
    throw new Error('내 예약 목록을 불러오는데 실패했습니다.');
  }
}
```

**🚨 서비스 계층의 핵심 문제:**
1. **잘못된 RPC 함수 사용**: `get_reservations_for_period` 대신 `get_user_reservations_detailed` 사용해야 함
2. **타입 캐스팅 오류**: 플랫 구조를 `ReservationWithDetails[]`로 강제 캐스팅
3. **클라이언트 사이드 필터링 의존**: 서버에서 이미 필터링된 데이터를 받아야 함

### 3.2 타입 정의 파일 (`src/types/database.ts`)

```typescript
// Extended types with relations
export type ReservationWithDetails = Reservation & {
  room: Room
  user: User
}

export type PublicReservation = {
  id: string
  room_id: string
  user_id: string
  title: string
  purpose: string | null
  department: string
  user_name: string // 예약자 이름 추가
  start_time: string
  end_time: string
  is_mine: boolean
}
```

**분석 결과:**
- ✅ `ReservationWithDetails`: 중첩된 `room`, `user` 객체 포함 - `get_user_reservations_detailed`와 일치
- ✅ `PublicReservation`: 플랫 구조 + `is_mine` - `get_reservations_for_period`와 일치
- 🚨 **타입 불일치**: 현재 코드는 `PublicReservation` 데이터를 `ReservationWithDetails`로 캐스팅하고 있음

---

## 🔐 Part 4: 접근 제어의 기초 - 데이터베이스 정책

### 4.1 reservations 테이블 RLS 정책

```sql
-- 인증된 사용자는 확정된 예약 조회 가능
CREATE POLICY "reservations_authenticated_read" ON public.reservations
    FOR SELECT
    TO authenticated
    USING (status = 'confirmed');

-- 예약 소유자는 모든 작업 가능
CREATE POLICY "reservations_owner_full_access" ON public.reservations
    FOR ALL
    TO authenticated
    USING (user_id IN (
        SELECT id FROM public.users WHERE auth_id = auth.uid()
    ))
    WITH CHECK (user_id IN (
        SELECT id FROM public.users WHERE auth_id = auth.uid()
    ));

-- 비인증 사용자도 확정된 예약 조회 가능 (익명화)
CREATE POLICY "reservations_public_read_confirmed" ON public.reservations
    FOR SELECT
    TO public
    USING (status = 'confirmed');
```

**분석 결과:**
- ✅ **적절한 권한 분리**: 소유자, 인증 사용자, 공개 사용자별 정책
- ✅ **보안 컨텍스트**: `SECURITY DEFINER` 함수에서 이 정책들이 올바르게 작동
- ✅ **데이터 접근 제어**: 사용자는 자신의 예약에만 전체 접근 가능

---

## 🎯 근본 원인 분석 및 해결 방안

### 🚨 주요 문제점 식별

1. **RPC 함수 불일치**:
   - 사용 중: `get_reservations_for_period` (플랫 구조)
   - 필요한 것: `get_user_reservations_detailed` (중첩 구조)

2. **타입 시스템 불일치**:
   - 기대: `ReservationWithDetails[]` (중첩 객체)
   - 실제: `PublicReservation[]` (플랫 구조)

3. **데이터 변환 로직 누락**:
   - 플랫 구조를 중첩 구조로 변환하는 로직 없음

### 🔧 해결 방안

#### 방안 1: RPC 함수 교체 (권장)

```typescript
// useMyReservations 훅 수정
const { data, error } = await supabase.rpc('get_user_reservations_detailed', {
  p_user_id: userId
});

if (error) {
  logger.error('get_user_reservations_detailed RPC failed', error);
  throw new Error(`내 예약 조회 실패: ${error.message}`);
}

// JSONB 응답을 파싱
const reservations = Array.isArray(data) ? data : [];
return reservations as ReservationWithDetails[];
```

#### 방안 2: 서비스 계층 수정

```typescript
async getMyReservations(supabase: SupabaseClient<Database>, userId: string): Promise<ReservationWithDetails[]> {
  try {
    const { data, error } = await supabase.rpc('get_user_reservations_detailed', {
      p_user_id: userId
    });

    if (error) throw error;
    
    // JSONB 배열을 직접 반환 (이미 올바른 구조)
    return data || [];
  } catch (error) {
    logger.error('내 예약 조회 실패', { error });
    throw new Error('내 예약 목록을 불러오는데 실패했습니다.');
  }
}
```

#### 방안 3: 쿼리 키 정리

```typescript
// reservationKeys.ts에서 일관된 키 사용
export const reservationKeys = {
  my: (userId?: string) => ['reservations', 'my', userId] as const,
  // ... 기타 키들
};
```

---

## 📋 최종 권장 조치사항

### 🚀 즉시 조치 (Critical)

1. **RPC 함수 교체**:
   - `useMyReservations` 훅에서 `get_user_reservations_detailed` 사용
   - 서비스 계층의 `getMyReservations` 함수 수정

2. **데이터베이스 함수 배포**:
   - 제공된 최신 `get_user_reservations_detailed` 함수 배포
   - 마이그레이션 파일 실행

### 🔧 단기 조치 (High Priority)

3. **타입 안전성 강화**:
   - JSONB 응답 파싱 로직 추가
   - 런타임 타입 검증 구현

4. **에러 처리 개선**:
   - RPC 함수 호출 실패 시 명확한 에러 메시지
   - 빈 결과와 에러 상황 구분

### 🎯 장기 개선 (Medium Priority)

5. **캐시 전략 최적화**:
   - 쿼리 키 일관성 확보
   - 적절한 staleTime 설정

6. **모니터링 강화**:
   - RPC 함수 호출 성공/실패 메트릭
   - 데이터 일관성 검증 로직

---

## 🏁 결론

**'내 예약' 기능의 불안정성은 RPC 함수 불일치와 타입 시스템 불일치가 주요 원인**입니다. 

현재 시스템은 `get_reservations_for_period` (플랫 구조)를 사용하여 데이터를 가져온 후 `ReservationWithDetails[]` (중첩 구조)로 캐스팅하고 있어, 실제 UI에서 필요한 `room.name` 등의 중첩 데이터에 접근할 수 없는 상황입니다.

제공해주신 `get_user_reservations_detailed` 함수로 교체하면, 올바른 데이터 구조를 반환받아 "예약이 없습니다" 문제를 근본적으로 해결할 수 있습니다.

**이 수정을 통해 RPC 엔진과 아키텍처 간의 '마지막 배선'이 완료되어, 시스템 전체의 최종 안정화를 달성할 수 있습니다.**