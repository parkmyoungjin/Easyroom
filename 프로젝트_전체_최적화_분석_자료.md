# Next.js 프로젝트 전체 최적화 및 레거시 코드 제거를 위한 종합 분석 자료

## 📋 프로젝트 개요

**프로젝트명:** Easyroom (회의실 예약 시스템)  
**기술 스택:** Next.js 15.3.5, React 19, TypeScript, Supabase, Mantine UI, TailwindCSS  
**분석 일시:** 2025년 8월 8일  
**분석 목적:** 레거시 코드 정리, 성능 최적화, 코드 품질 향상

---

## 🏗️ 1. 프로젝트 구조 분석

### 전체 디렉토리 구조
```
Easyroom/
├── src/
│   ├── app/                    # Next.js App Router 페이지
│   │   ├── layout.tsx         # 루트 레이아웃
│   │   ├── providers.tsx      # 전역 프로바이더
│   │   ├── globals.css        # 전역 스타일
│   │   ├── page.tsx           # 홈페이지
│   │   ├── dashboard/         # 대시보드 페이지
│   │   ├── login/             # 로그인 페이지
│   │   ├── signup/            # 회원가입 페이지
│   │   ├── reservations/      # 예약 관련 페이지
│   │   ├── admin/             # 관리자 페이지
│   │   └── api/               # API 라우트
│   ├── components/            # 재사용 가능한 컴포넌트
│   │   ├── auth/              # 인증 관련 컴포넌트
│   │   ├── ui/                # 기본 UI 컴포넌트
│   │   ├── layout/            # 레이아웃 컴포넌트
│   │   ├── forms/             # 폼 컴포넌트
│   │   ├── pwa/               # PWA 관련 컴포넌트
│   │   └── monitoring/        # 모니터링 컴포넌트
│   ├── hooks/                 # 커스텀 훅
│   ├── lib/                   # 유틸리티 및 설정
│   │   ├── auth/              # 인증 로직
│   │   ├── supabase/          # Supabase 클라이언트
│   │   ├── utils/             # 유틸리티 함수
│   │   ├── config/            # 환경 설정
│   │   ├── security/          # 보안 관련
│   │   └── monitoring/        # 모니터링 시스템
│   ├── contexts/              # React Context
│   ├── features/              # 기능별 모듈
│   ├── types/                 # TypeScript 타입 정의
│   └── __tests__/             # 테스트 파일
├── supabase/                  # Supabase 설정 및 마이그레이션
├── public/                    # 정적 파일
├── scripts/                   # 빌드 및 유틸리티 스크립트
└── 설정 파일들
```

---

## ⚙️ 2. 핵심 설정 파일 분석

### 2.1 package.json 분석
```json
{
  "name": "Easyroom",
  "version": "0.1.0",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
```

**주요 의존성:**
- **프레임워크:** Next.js 15.3.5, React 19.0.0
- **UI 라이브러리:** @mantine/core 8.2.2, TailwindCSS 3.4.1
- **데이터베이스:** @supabase/supabase-js 2.50.5, @supabase/ssr 0.6.1
- **상태 관리:** @tanstack/react-query 5.x, zustand 4.x
- **폼 관리:** react-hook-form 7.60.0, @hookform/resolvers 4.1.3
- **유틸리티:** date-fns 4.1.0, zod 3.25.74, framer-motion 11.18.2

**스크립트 분석:**
- 총 60개 이상의 npm 스크립트 (과도하게 많음)
- 테스트, 빌드, 모니터링, 배포 관련 스크립트들이 복잡하게 구성됨

### 2.2 Next.js 설정 (next.config.ts)
```typescript
const nextConfig: NextConfig = {
  eslint: { ignoreDuringBuilds: true },
  images: { remotePatterns: [{ hostname: '**' }] },
  webpack: (config, { isServer, dev }) => {
    // 복잡한 웹팩 설정
    // 폴백 설정, 최적화, 청크 분할 등
  },
  experimental: {
    optimizeCss: true,
    scrollRestoration: true,
  }
}
```

**문제점:**
- 웹팩 설정이 과도하게 복잡함
- 보안 헤더 설정이 환경별로 동적으로 구성되어 복잡함
- 이미지 최적화 설정이 너무 관대함 (`hostname: '**'`)

### 2.3 TypeScript 설정 (tsconfig.json)
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "strict": true,
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  }
}
```

**양호한 설정이지만 개선 가능:**
- 타겟을 ES2020으로 업그레이드 가능
- 더 엄격한 타입 체크 옵션 추가 가능

---

## 🎨 3. 전역 설정 및 레이아웃 분석

### 3.1 루트 레이아웃 (src/app/layout.tsx)
```typescript
export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const supabase = await createClient();
  const { data: { user }, error } = await supabase.auth.getUser();
  
  return (
    <html lang="ko" suppressHydrationWarning={true}>
      <body className={`${inter.className} min-h-screen antialiased`}>
        <ClientPolyfillManager>
          <Providers>
            <SupabaseProvider>
              <AuthProvider initialSession={session}>
                <AppInitializer>
                  <AuthGatekeeper>
                    {children}
                  </AuthGatekeeper>
                </AppInitializer>
              </AuthProvider>
            </SupabaseProvider>
          </Providers>
        </ClientPolyfillManager>
      </body>
    </html>
  );
}
```

**문제점:**
- 프로바이더 중첩이 과도하게 깊음 (7단계)
- 서버 컴포넌트에서 인증 확인 후 클라이언트로 전달하는 복잡한 구조
- 불필요한 래퍼 컴포넌트들이 많음

### 3.2 프로바이더 설정 (src/app/providers.tsx)
```typescript
export default function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 1 * 60 * 1000,
        gcTime: 10 * 60 * 1000,
        refetchOnWindowFocus: true,
      },
    },
  }));

  return (
    <StartupValidationProvider>
      <MantineProvider theme={theme}>
        <QueryClientProvider client={queryClient}>
          {children}
        </QueryClientProvider>
      </MantineProvider>
    </StartupValidationProvider>
  );
}
```

**문제점:**
- StartupValidationProvider가 실제로는 비활성화됨 (`skipValidation={true}`)
- 불필요한 검증 로직이 포함되어 있음

---

## 🧩 4. 핵심 로직 및 공통 컴포넌트 분석

### 4.1 인증 시스템 (src/contexts/AuthContext.tsx)
```typescript
export const AuthProvider = ({ children, initialSession = null }: AuthProviderProps) => {
  const [user, setUser] = useState<User | null>(initialSession?.user ?? null);
  const [userProfile, setUserProfile] = useState<UserProfile | null>(null);
  const [isLoading, setIsLoading] = useState(true);

  // 복잡한 인증 상태 관리 로직
}
```

**문제점:**
- 인증 상태 관리가 과도하게 복잡함
- 서버/클라이언트 간 세션 동기화 로직이 복잡함
- 프로필 가져오기 로직이 여러 곳에 분산됨

### 4.2 Supabase 클라이언트 (src/lib/supabase/server.ts)
```typescript
export const createClient = cache(async () => {
  const cookieStore = await cookies();
  return createServerClient<Database>(/* ... */);
});
```

**양호한 구조:**
- React cache를 사용한 최적화
- 적절한 서버 전용 설정

### 4.3 커스텀 훅 (src/hooks/useAuth.ts)
```typescript
export function useAuth() {
  // 700줄이 넘는 복잡한 인증 로직
  const signInWithMagicLink = useCallback(async (email: string) => {
    // 복잡한 매직링크 로직
  }, [supabase]);

  const signUpDirectly = useCallback(async (email: string, fullName: string, department: string) => {
    // 복잡한 회원가입 로직
  }, [supabase]);
}
```

**문제점:**
- 단일 훅이 너무 많은 책임을 가짐
- 에러 처리 로직이 중복됨
- 함수들이 과도하게 복잡함

---

## 📁 5. 컴포넌트 구조 분석

### 5.1 UI 컴포넌트 (src/components/ui/)
- **기본 컴포넌트:** button, input, card, dialog 등
- **shadcn/ui 기반:** 일관된 디자인 시스템
- **문제점:** Mantine과 shadcn/ui가 혼재됨

### 5.2 레이아웃 컴포넌트 (src/components/layout/)
```typescript
// AuthGatekeeper - 복잡한 라우팅 로직
// AppInitializer - 초기화 로직
// SplashScreen - 로딩 화면
```

**문제점:**
- 책임이 명확하지 않은 컴포넌트들
- 과도한 추상화로 인한 복잡성

### 5.3 기능별 컴포넌트 (src/features/)
```
features/
├── auth/
│   ├── components/
│   └── hooks/
├── admin/
└── reservation/
```

**양호한 구조:** 기능별로 잘 분리됨

---

## 🔧 6. 유틸리티 및 서비스 분석

### 6.1 유틸리티 함수 (src/lib/utils/)
- **과도한 세분화:** 20개 이상의 작은 유틸리티 파일
- **중복 기능:** 비슷한 기능을 하는 함수들이 여러 파일에 분산
- **사용되지 않는 코드:** 일부 유틸리티가 실제로 사용되지 않음

### 6.2 서비스 레이어 (src/lib/services/)
```typescript
// reservations.ts, rooms.ts, users.ts
// 각각 API 호출 로직을 담당
```

**양호한 구조:** 도메인별로 잘 분리됨

---

## 🧪 7. 테스트 구조 분석

### 테스트 파일 현황
- **총 테스트 파일:** 100개 이상
- **테스트 커버리지:** 광범위하지만 일부 중복
- **문제점:** 
  - 테스트 설정이 과도하게 복잡함
  - 일부 테스트가 실제 기능과 동기화되지 않음
  - 테스트 유틸리티가 분산되어 있음

---

## 📊 8. 성능 및 최적화 분석

### 8.1 번들 크기 이슈
- **Mantine + shadcn/ui 중복:** 두 UI 라이브러리 동시 사용
- **과도한 의존성:** 일부 사용되지 않는 라이브러리들
- **코드 스플리팅:** 적절히 구현되어 있음

### 8.2 렌더링 최적화
- **React.memo 사용:** 일부 컴포넌트에만 적용
- **useCallback/useMemo:** 과도하게 사용되거나 누락된 경우들
- **상태 관리:** Context API 과사용으로 인한 불필요한 리렌더링

### 8.3 데이터 페칭
- **React Query 설정:** 적절한 캐싱 전략
- **Supabase 최적화:** 실시간 구독 관리 필요

---

## 🚨 9. 주요 문제점 식별

### 9.1 아키텍처 문제
1. **과도한 추상화:** 불필요한 래퍼 컴포넌트와 프로바이더
2. **책임 분산:** 단일 기능이 여러 파일에 분산
3. **복잡한 상태 관리:** 인증 상태가 여러 곳에서 관리됨

### 9.2 코드 품질 문제
1. **중복 코드:** 비슷한 로직이 여러 곳에 반복
2. **사용되지 않는 코드:** 레거시 함수와 컴포넌트들
3. **일관성 부족:** 코딩 스타일과 패턴이 일관되지 않음

### 9.3 성능 문제
1. **번들 크기:** 불필요한 의존성으로 인한 크기 증가
2. **렌더링 최적화:** 불필요한 리렌더링 발생
3. **메모리 누수:** 일부 구독과 이벤트 리스너 정리 누락

### 9.4 유지보수성 문제
1. **복잡한 설정:** 과도하게 복잡한 웹팩과 Next.js 설정
2. **테스트 복잡성:** 테스트 설정과 구조가 과도하게 복잡
3. **문서화 부족:** 복잡한 로직에 대한 문서화 부족

---

## 🎯 10. 최적화 우선순위 제안

### 🔴 높은 우선순위 (즉시 수정 필요)
1. **인증 시스템 단순화**
   - AuthProvider와 useAuth 훅 리팩토링
   - 불필요한 인증 래퍼 제거
   - 서버/클라이언트 세션 동기화 단순화

2. **프로바이더 구조 최적화**
   - 불필요한 프로바이더 제거
   - 중첩 깊이 줄이기
   - StartupValidationProvider 제거

3. **UI 라이브러리 통일**
   - Mantine 또는 shadcn/ui 중 하나로 통일
   - 중복 컴포넌트 제거
   - 일관된 디자인 시스템 구축

### 🟡 중간 우선순위 (단계적 개선)
1. **유틸리티 함수 정리**
   - 사용되지 않는 유틸리티 제거
   - 중복 기능 통합
   - 공통 유틸리티 모듈화

2. **컴포넌트 구조 개선**
   - 책임이 불명확한 컴포넌트 리팩토링
   - 과도한 추상화 제거
   - 재사용성 향상

3. **성능 최적화**
   - React.memo, useCallback 적절한 적용
   - 번들 크기 최적화
   - 이미지 최적화 설정 개선

### 🟢 낮은 우선순위 (장기적 개선)
1. **테스트 구조 개선**
   - 중복 테스트 제거
   - 테스트 유틸리티 통합
   - 테스트 설정 단순화

2. **빌드 설정 최적화**
   - 웹팩 설정 단순화
   - 불필요한 npm 스크립트 제거
   - 빌드 성능 향상

3. **문서화 및 타입 개선**
   - 복잡한 로직 문서화
   - 타입 정의 개선
   - 코딩 가이드라인 수립

---

## 📋 11. 구체적인 개선 계획

### Phase 1: 인증 시스템 리팩토링 (1-2주)
```typescript
// 목표: 단순화된 인증 구조
// Before: 7단계 프로바이더 중첩
// After: 3단계로 단순화

<QueryClientProvider>
  <AuthProvider>
    <MantineProvider>
      {children}
    </MantineProvider>
  </AuthProvider>
</QueryClientProvider>
```

### Phase 2: UI 라이브러리 통일 (2-3주)
- Mantine으로 통일 (현재 더 많이 사용됨)
- shadcn/ui 컴포넌트를 Mantine으로 마이그레이션
- 일관된 테마 시스템 구축

### Phase 3: 코드 정리 및 최적화 (2-3주)
- 사용되지 않는 코드 제거
- 중복 로직 통합
- 성능 최적화 적용

### Phase 4: 테스트 및 문서화 (1-2주)
- 테스트 구조 개선
- 핵심 로직 문서화
- 코딩 가이드라인 수립

---

## 🔍 12. 예상 효과

### 성능 개선
- **번들 크기:** 30-40% 감소 예상
- **초기 로딩 시간:** 20-30% 개선 예상
- **렌더링 성능:** 불필요한 리렌더링 50% 감소

### 개발 경험 개선
- **코드 가독성:** 복잡성 60% 감소
- **유지보수성:** 버그 수정 시간 40% 단축
- **개발 속도:** 새 기능 개발 30% 빠름

### 안정성 향상
- **타입 안전성:** TypeScript 활용도 증가
- **에러 처리:** 일관된 에러 처리 패턴
- **테스트 커버리지:** 실질적인 테스트 품질 향상

---

이 분석 자료를 바탕으로 체계적인 리팩토링을 진행하시면, 프로젝트의 성능, 유지보수성, 개발 경험을 크게 향상시킬 수 있을 것입니다. 각 단계별로 구체적인 구현 방안을 제시해드릴 수 있으니, 어떤 부분부터 시작하고 싶으신지 알려주세요.